# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/),
and this project adheres to [Semantic Versioning](https://semver.org/).

## [Unreleased]

### Changed

- [#588] Split category into base_category + subcategory so AppBlock subclasses with custom palette labels retain correct base type detection (@claude, 2026-04-11, branch: refactor/issue-588/category-subcategory-split, session: 20260411-042819-split-category-into-base-category-subcat)

### Fixed

- [#581] Remove redundant fiji_path/napari_path fields, inject ClassVar defaults into config_schema, remove auto-open file manager, add copy button feedback (@claude, 2026-04-11, branch: fix/issue-581/path-cleanup-copy-feedback)
- [#580] Add WORKFLOW_STARTED to WebSocket outbound events so frontend Run button spinner activates (@claude, 2026-04-11, branch: fix/issue-580/workflow-started-ws, session: 20260411-032524-fix-580-add-workflow-started-to-websocke)

### Changed

- [#579] Fix 7 project management bugs: browse button, sort by last_opened, prune deleted, rename Save Project, delete button, clear canvas on open, direct recent open, AppBlock output_dir default (@claude, 2026-04-11, branch: fix/issue-579/project-management-bugs, session: 20260411-032627-fix-7-project-management-bugs-579)
- [#574] Remove CellProfiler/QuPath blocks, add subcategory labels to Imaging blocks, enable category override in registry (@claude, 2026-04-11, branch: refactor/issue-574/imaging-cleanup-subcategories, session: 20260411-021002-remove-cellprofiler-qupath-blocks-add-su)
- [#571] Inject Executable Path (file_browser) + Save Outputs At with forced ui_priority ordering in AppBlock (@claude, 2026-04-11, branch: fix/issue-571/appblock-config-injection, session: 20260411-020155-fix-571-inject-executable-path-save-outp)
- [#572] Clean up Fiji config after AppBlock MRO injection: remove redundant watch_timeout from config_schema (@claude, 2026-04-11, branch: fix/issue-572/fiji-elmaven-config-cleanup, session: 20260411-020318-clean-up-fiji-and-elmaven-config-after-a)
- [#561] Fix Windows browse dialog bugs: PowerShell double-brace syntax, add multi-file select, modernize folder dialog, update frontend to paths array (@claude, 2026-04-11, branch: fix/issue-561/browse-dialog-bugs, session: 20260411-020115-fix-browse-dialog-bugs-on-windows-561)
- [#569] Add ADR-029 variadic ports implementation roadmap with 8 detailed tickets (@claude, 2026-04-11, branch: docs/issue-569/adr-029-variadic-ports-roadmap, session: 20260411-013533-docs-adr-029-variadic-ports-implementati)
- [#555] Rewrite ElMAVEN block to follow standard AppBlock pattern -- fixes rerun deadlock (@claude, 2026-04-10, branch: refactor/issue-555/elmaven-standard-pattern, session: 20260410-005711-rewrite-elmaven-block-to-follow-standard)
- [#541] LCMS process block audit: remove 9 premature/skeleton blocks, fix config labels, improve flux estimate with linregress (@claude, 2026-04-10, branch: refactor/issue-541/lcms-process-audit, session: 20260410-002151-refactor-lcms-lcms-process-block-audit-r)
- [#540] LCMS IO block audit: rename LoadMSRawFiles to LoadMzMLFiles, remove regex config from LoadMIDTable (@claude, 2026-04-10, branch: refactor/issue-540/lcms-io-audit, session: 20260410-001346-refactor-lcms-lcms-io-block-audit-rename)
- [#545] Show spinning indicator on Run button during workflow execution (@claude, 2026-04-10, branch: feat/issue-545/run-button-spinner, session: 20260410-001118-show-spinning-indicator-on-run-button-du)
- [#542] SRS block audit — remove Band Ratio and SRS Normalize, rename Denoise to Spectral Denoise with Savitzky-Golay, add Calibrate ui_priority, add Unmix mode selector (@claude, 2026-04-10, branch: refactor/issue-542/srs-block-audit, session: 20260410-000652-srs-block-audit-remove-band-ratio-and-sr)
- [#530] Core block palette audit: remove base classes and placeholders from palette, rename Load Data/Save Data to Load/Save, add SubWorkflow config_schema (@claude, 2026-04-10, branch: fix/issue-530/palette-audit, session: 20260410-000636-refactor-gui-core-block-palette-audit-re)
- [#538] LCMS app block audit: ElMAVEN cleanup, remove GraphPad skeleton, remove watch_timeout (@claude, 2026-04-10, branch: refactor/issue-538/lcms-app-audit, session: 20260410-000747-refactor-lcms-lcms-app-block-audit-elmav)
- [#464] Improve preview panel: dark-bg image viewer with pan/zoom/LUT, compact table viewer with formatting (@claude, 2026-04-09, branch: feat/issue-464/preview-viewer, session: 20260409-124853-improve-preview-image-table-viewers-464)
- [#476] Cache Python site-packages in CI to reduce install overhead (@claude, 2026-04-09, branch: perf/issue-476/ci-pip-cache, session: 20260409-124905-optimize-ci-pip-install-caching)
- [#468] Optimize CI: parallel jobs, single-version coverage, pytest-xdist (@claude, 2026-04-09, branch: perf/issue-468/ci-speedup, session: wave2-ci-perf)
- [#474] Reduce test sleep durations and stability periods to eliminate CI stall at 88% (@claude, 2026-04-09, branch: perf/issue-474/reduce-test-sleeps, session: 20260409-123838-fix-ci-88-stall-reduce-test-sleeps)

### Added

- [#567] Promote ADR-029 variadic port count to proposed with Q1-Q11 decisions (D1-D13), update ARCHITECTURE.md (@claude, 2026-04-11, branch: docs/issue-567/adr-029-promote-proposed, session: 20260411-011559-promote-adr-029-to-proposed-with-variadi)
- [#557] ADR-030 config_schema MRO merge: auto-inherit base class config fields, IOBlock path injection, AppBlock output_dir, directory_browser copy button, PAUSED toast (@claude, 2026-04-10, branch: feat/issue-557/config-schema-mro-merge, session: 20260410-024235-adr-030-config-schema-mro-merge-ioblock)
- [#543] Auto-assign port colors for plugin types via deterministic hashing (@claude, 2026-04-10, branch: feat/issue-543/port-color-hashing, session: 20260410-001558-feat-gui-auto-assign-port-colors-for-plu)
- [#531] AI block: large prompt textarea widget + load prompt from file (.md/.txt) (@claude, 2026-04-10, branch: fix/issue-531/ai-block-textarea, session: 20260410-001127-feat-gui-ai-block-large-prompt-textarea)
- [#494] Add native OS file/directory dialog for browse button with platform-specific subprocess calls (@claude, 2026-04-09, branch: feat/issue-494/native-file-dialog, session: 20260409-165203-native-os-file-dialog-for-browse-button)
- [#497] Add multi-workflow tabs: open multiple workflows as tabs with per-tab state, dirty indicators, and close confirmation (@claude, 2026-04-09, branch: feat/issue-497/multi-workflow-tabs, session: 20260409-165642-multi-workflow-tabs-in-frontend-462)
- [#484] Add universal filesystem browse button for all path config fields via ui_widget schema annotation (@claude, 2026-04-09, branch: feat/issue-484/universal-browse, session: 20260409-154209-universal-filesystem-browse-button-484)
- [#465] Add project tree tab in left sidebar with lazy-loading file browser, right-click menu, and reveal-in-explorer (@claude, 2026-04-09, branch: feat/issue-465/project-tree, session: 20260409-150547-project-tree-tab-in-left-sidebar-465)
- [#452] Implement AIBlock MVP: functional `run()` with LLM provider integration, input serialization, prompt templating, and Text output (@claude, 2026-04-08, branch: feat/issue-452/ai-block-mvp, session: 20260408-224820-feat-ai-aiblock-mvp-implementation)
- [#446] Add workflow management toolbar: New, Save, Save As, Import buttons replace Export; backend endpoints for YAML workflow import and file browsing (@claude, 2026-04-08, branch: feat/issue-446/workflow-management, session: 20260408-220724-feat-gui-workflow-management-save-new-im)
- [#445] Visual polish: solid colored port handles by data type with subtype ring colors, solid edge lines, higher-contrast palette tags, collapsible type legend, updated landing tagline (@claude, 2026-04-08, branch: feat/visual-polish-445, session: 20260408-220708-feat-gui-visual-polish-445)

### Fixed

- [#338,#339] Fix AppBlock subprocess leak (no proc.wait after run) and temp exchange directory leak (tempfile.mkdtemp never cleaned up) (@claude, 2026-04-11, branch: fix/issue-338-339/appblock-resource-leaks, session: 20260411-020206-fix-appblock-subprocess-leak-338-and-tem)
- [#565] Fix BlockConfig.get() to find enriched runtime keys (block_id, project_dir); add data/exchange to create_project (@claude, 2026-04-10, branch: fix/issue-565/blockconfig-get-extra-fields, session: 20260410-045525-fix-blockconfig-get-not-finding-enriched)
- [#563] Pass raw file paths as CLI arguments to ElMAVEN so files are pre-loaded on launch (@claude, 2026-04-10, branch: fix/issue-563/elmaven-file-args, session: 20260410-040343-fix-563-elmaven-block-does-not-pass-inpu)
- [#534] Remove is_collection double-ring from port rendering (@claude, 2026-04-10, branch: fix/issue-534/remove-collection-double-ring, session: 20260410-000721-fix-gui-remove-is-collection-double-ring)
- [#525] Switch LCMS load blocks from directory_browser/glob to file_browser with multi-file support (@claude, 2026-04-09, branch: fix/issue-525/load-blocks-file-browser, session: N/A)
- [#526] Fix ElMAVEN block freeze — remove unused argv_override CLI args, fix PIPE deadlock with DEVNULL, add proc.wait (@claude, 2026-04-09, branch: fix/issue-526/elmaven-cli-pipe-deadlock, session: N/A)
- [#524] Fix toolbar layout shift — use fixed-width name area and visibility-based dirty indicator (@claude, 2026-04-09, branch: fix/issue-524/toolbar-fixed-width, session: N/A)
- [#520] Fix block error messages truncated — add expandable error display with tooltip, Problems tab, and copy button (@claude, 2026-04-09, branch: fix/issue-507/expandable-block-errors, session: 20260409-192041-block-error-messages-truncated-need-expa)
- [#517] Fix TabBar invisible on startup — always render tab bar, use openTab for new projects (@claude, 2026-04-09, branch: fix/issue-503/tabbar-startup, session: 20260409-191833-fix-tabbar-invisible-on-startup-no-initi)
- [#510] Fix ElMAVEN block to pass raw file paths as CLI arguments so data is pre-loaded (@claude, 2026-04-09, branch: fix/issue-510/elmaven-data-injection, session: 20260409-192253-elmaven-block-opens-without-input-data-5)
- [#506] Store relative paths in workflow YAML for cross-machine portability (@claude, 2026-04-09, branch: fix/issue-506/relative-paths, session: 20260409-191659-store-relative-paths-in-workflow-yaml-50)
- [#509] Disable monorepo scanner for end users — gate behind SCIEASY_DEV=1 env var (@claude, 2026-04-09, branch: fix/issue-509/disable-monorepo-scanner, session: 20260409-191427-disable-monorepo-scanner-for-end-users-5)
- [#499] Configure Python logging handlers so logger output is not silently discarded (@claude, 2026-04-09, branch: fix/issue-499/configure-logging, session: 20260409-180335-configure-logging-handlers-for-the-codeb)
- [#511] Fix toolbar layout shift caused by long workflow names — add max-width truncation with tooltip (@claude, 2026-04-09, branch: fix/issue-508/toolbar-name-truncation, session: 20260409-191332-fix-toolbar-shifts-when-workflow-filenam)
- [#488] Fix block palette: group core blocks under SciEasy Core, register LCMS/SRS plugins with entry-points (@claude, 2026-04-09, branch: fix/issue-488/palette-categorization, session: 20260409-180931-fix-block-palette-core-blocks-scattered)
- [#495] Fix ResourceManager memory watermark deadlock on macOS and PosixOps pid=-1 alive check bug (@claude, 2026-04-09, branch: fix/issue-495/memory-watermark-deadlock, session: 20260409-180231-fix-resourcemanager-memory-watermark-dea)
- [#461] Fix palette categorization: add type_name prefixes to LCMS/SRS blocks, refine subcategories, populate source/package_name for Custom group (@claude, 2026-04-09, branch: fix/issue-461/palette-categorization, session: 20260409-124810-fix-block-palette-categorization-461)
- [#483] Fix macOS workflow execution: async subprocess, .app bundle support, checkpoint error message (@claude, 2026-04-09, branch: fix/issue-483/macos-execution, session: 20260409-151529-fix-macos-workflow-execution-483)
- [#467] Fix macOS crash: ensure workflow dir exists + remove tkinter file dialogs (@claude, 2026-04-09, branch: fix/issue-467/macos-crash, session: wave2-macos-fix)
- [#457] Fix toolbar button shift on dirty state toggle and block node config overflow with ellipsis truncation (@claude, 2026-04-09, branch: fix/issue-457/toolbar-node-overflow, session: wave1-gui-fixes)
- [#463] Allow parentheses in command validator for Windows paths like `Program Files (x86)` (@claude, 2026-04-09, branch: fix/issue-463/command-validator-parens, session: wave1-gui-fixes)
- [#466] Fix MiniMap rendering blank white by setting visible maskColor, background, and darker node color fallback (@claude, 2026-04-09, branch: fix/issue-466/minimap-blank, session: wave1-gui-fixes)
- [#463] Allow parentheses in AppBlock command validator for Windows paths like `C:\Program Files (x86)\` (@claude, 2026-04-09, branch: fix/issue-463/command-validator-parens, session: wave1-gui-fixes)
- [#433] Allow dragging block nodes from any position, not just the title bar (@claude, 2026-04-08, branch: fix/frontend-fixes-433-434-438, session: 20260408-211501-fix-gui-frontend-ux-fixes-for-433-434-43)
- [#434] Multi-file selection now sends full paths array instead of collapsing to parent directory (@claude, 2026-04-08, branch: fix/frontend-fixes-433-434-438, session: 20260408-211501-fix-gui-frontend-ux-fixes-for-433-434-43)
- [#438] Image preview uses 256px downsampling instead of 64x64 crop; Label/CompositeData raster slot renders as image preview (@claude, 2026-04-08, branch: fix/frontend-fixes-433-434-438, session: 20260408-211501-fix-gui-frontend-ux-fixes-for-433-434-43)
- [#424] Add `_cancel_if_active()` and `rerun_block()` to `DAGScheduler`; update `reset_block` and `execute_from` to cancel active tasks/subprocesses before resetting state, preventing orphan processes on rerun (@claude, 2026-04-08, branch: fix/engine-core-fixes-424-435-436-437, session: 20260408-211448-fix-engine-core-engine-fixes-for-424-435)
- [#435] Remove unsafe fallback in `_gather_inputs()` that passed entire upstream output dict when port key was missing; log warning and skip instead (@claude, 2026-04-08, branch: fix/engine-core-fixes-424-435-436-437, session: 20260408-211448-fix-engine-core-engine-fixes-for-424-435)
- [#436] Add recursive CompositeData slot flushing in `_auto_flush()` so child DataObjects persist to storage before crossing subprocess boundary (@claude, 2026-04-08, branch: fix/engine-core-fixes-424-435-436-437, session: 20260408-211448-fix-engine-core-engine-fixes-for-424-435)
- [#437] Verify `validate_connection()` subclass matching via `issubclass()` is correct; add comprehensive regression tests for port type inheritance (Mask -> Image) (@claude, 2026-04-08, branch: fix/engine-core-fixes-424-435-436-437, session: 20260408-211448-fix-engine-core-engine-fixes-for-424-435)
- [#432] Log exchange output directory at INFO level and auto-open OS file manager when `AppBlock` transitions to PAUSED so users know where to save files (@claude, 2026-04-08, branch: fix/imaging-plugin-fixes-432-434-439, session: 20260408-211515-fix-imaging-plugin-fixes-for-432-434-439)
- [#434] Support batch save in `SaveImage` for multi-item `Collection[Image]`; path is treated as directory with auto-numbered filenames (`image_0000.tif`, etc.) (@claude, 2026-04-08, branch: fix/imaging-plugin-fixes-432-434-439, session: 20260408-211515-fix-imaging-plugin-fixes-for-432-434-439)
- [#439] Add dual output to `CellposeSegment`: new `masks` output port emits raw integer-label `Image` objects alongside the existing `labels` port (@claude, 2026-04-08, branch: fix/imaging-plugin-fixes-432-434-439, session: 20260408-211515-fix-imaging-plugin-fixes-for-432-434-439)
- [#420] Fix `FijiBlock` to pass staged TIFF file paths as trailing CLI args to Fiji instead of the exchange directory root; add `argv_override` to `FileExchangeBridge.launch()` and `launch_args` to `_run_external_app()` helper; macro mode continues to pass `exchange_dir` unchanged (@claude, 2026-04-08, branch: fix/issue-420/fiji-pass-image-paths, session: 20260408-190818-fix-fijiblock-passes-exchange-dir-instea)
- [#419] Fix `CellposeSegment.setup()` to detect cellpose v3+ API at runtime: use `models.Cellpose` when present (legacy v2), fall back to `models.CellposeModel(model_type=...)` when the wrapper is absent; emit DEBUG log on fallback path (@claude, 2026-04-08, branch: fix/issue-419/cellpose-api-compatibility, session: 20260408-190544-fix-cellposesegment-legacy-cellpose-mode)
- [#422] Surface block error messages on node indicator and GUI log panel: inline error text (80-char truncation, full text on hover) appears next to the error badge on BlockNode; full error is appended to the Logs tab via `appendLog` on `block_error` WebSocket events; `blockErrors` map added to `ExecutionSlice` (@claude, 2026-04-08, branch: fix/issue-422/surface-block-errors-gui, session: 20260408-190702-surface-block-error-messages-on-node-and)
- [#421] Support multi-file selection as Collection output in `LoadData` and `LoadImage` blocks: `path` config field now accepts a string or a list of strings; when a list is supplied each file is loaded and results are packed into a homogeneous `Collection`; single-path behavior is fully preserved (@claude, 2026-04-08, branch: fix/issue-421/load-multi-file-collection, session: 20260408-190602-fix-load-loadimage-multi-file-selection)
- [#416] Activate worker auto-flush context during output serialization so in-memory imaging outputs persist to storage before crossing subprocess boundaries, preventing downstream `Image.to_memory()` failures in interactive blocks like Fiji (@Codex, 2026-04-08, branch: codex/fix-416-worker-autoflush, session: 20260408-181444-fix-416-worker-serialise-outputs-activat)
- [#408] Deprecate `deserialize_intermediate_refs()` and `_deserialize_value()` in `checkpoint.py` with detailed docstrings explaining the execute-from wire-format pass-through contract; add explanatory comment in `execute_from()` in `scheduler.py` (@claude, 2026-04-08, branch: fix/issue-408/deprecate-dead-deserialize, session: 20260408-154723-chore-deprecate-dead-deserialize-interme)
- [#405] Fix `preview_data()` to use `TypeRegistry.resolve()` + `issubclass()` for plugin type dispatch, replacing hardcoded class name comparisons and the "Spectrum" substring hack; add `DataRecord.type_chain` field (@claude, 2026-04-08, branch: fix/issue-405/preview-type-identity, session: 20260408-154724-fix-405-preview-runtime-drops-plugin-typ)
- [#404] Fix `_deserialize_value()` in `checkpoint.py` to read `metadata.type_chain` when building `TypeSignature` for `ViewProxy`, preserving plugin type identity for both single-object and collection-item wire-format dicts; add regression tests (@claude, 2026-04-08, branch: fix/issue-404/checkpoint-wire-format, session: 20260408-154723-fix-404-execute-from-checkpoint-typed-pa)
- [#407] Fix `_infer_type_name_from_ref` to honour `type_chain` from worker wire-format metadata; add `DataRecord.type_chain` field; replace hardcoded class name comparisons in `preview_data` with `TypeRegistry.resolve()` + `issubclass()` dispatch, removing the "Spectrum" substring hack (@claude, 2026-04-08, branch: fix/issue-407/infer-type-name-type-chain, session: 20260408-154720-fix-infer-type-name-from-ref-ignores-met)
- [#396] Restore plugin IO execution/schema fidelity by surfacing block-run failures in the GUI, exposing `direction` and `is_collection` in block schemas, aligning imaging collection ports to item-type metadata, and adding monorepo plugin block/type discovery so local source checkouts still render correct colors and Browse actions (@Codex, 2026-04-08, branch: fix/issue-396/gui-io-metadata, session: 20260408-133541-fix-issue-396-gui-io-metadata-and-execut)

### Added

- [#423] Upgrade right-side preview panel: image preview now has zoom/pan (mouse-wheel + drag), brightness/contrast sliders, and colormap/LUT selector; table preview now has scrollable container, sticky column headers, and row×column count display (@claude, 2026-04-08, branch: feat/issue-423/upgrade-preview-inspection-tools, session: 20260408-191640-upgrade-right-side-image-and-table-previ)
- [#425] Block Palette now renders a 3-level collapsible tree (package → category → block), deriving package names from `type_name` prefix or `package_name` field; "Custom" blocks sort to the bottom; search auto-collapses empty branches; all Stage 10.1 Part 2 test stubs implemented and passing (@claude, 2026-04-08, branch: feat/issue-425/palette-multi-level-package-grouping, session: 20260408-190740-make-block-palette-multi-level-grouped-b)
- [#401] Bump the core `scieasy` package to 0.2.1 and align plugin dependency floors to `scieasy>=0.2.1`, fixing local editable-install failures caused by version drift between the root package metadata and Phase 11 plugin packaging metadata (@Codex, 2026-04-08, branch: fix/issue-401/bump-core-to-0.2.1, session: 20260408-143236-bump-scieasy-core-package-version-to-0-2)
- [#398] Add `docs/testing/AI_AGENT_SYSTEM_TESTING_GUIDE.md`, a formal English system-test playbook for AI agents covering the fixed K562 imaging dataset, CLI runtime validation, browser-tool-driven GUI execution from `pip install`, hyperspectral/SRS coverage, pause/resume/checkpoint verification, and required Windows/macOS parity (@Codex, 2026-04-08, branch: docs/issue-398/ai-agent-system-testing-guide, session: 20260408-140515-formalize-ai-agent-system-testing-guide)
- [#389] Auto-bundle frontend SPA on `pip install` via setuptools `build_py` hook + dev-mode fallback in `api/app.py` so `scieasy gui` serves the React workflow editor instead of the FastAPI /docs panel (@claude, 2026-04-08, branch: feat/issue-389/bundle-frontend-spa, session: 20260408-113948-serve-frontend-spa-automatically-on-pip)
- [#386] Implement remaining imaging blocks: preprocess B + registration + projection + math + visualization + interactive AppBlocks + packaging (T-IMG-008..038) (@codex, 2026-04-08, branch: feat/sprint-c/imaging-finish, session: 20260408-080833-sprint-c-imaging-finish-t-img-008-038)
- [#385] Implement SRS component_analysis blocks (T-SRS-006..010) (@claude, 2026-04-08, branch: feat/sprint-d/srs-component-analysis, session: 20260408-080859-sprint-d-srs-component-analysis-t-srs-00)
- [#377] Sprint D SRS preprocess chunk impl - replaces the T-SRS-002 `SRSCalibrate`, T-SRS-003 `SRSBaseline`, T-SRS-004 `SRSDenoise`, and T-SRS-005 `SRSNormalize` skeleton bodies with concrete implementations inside `packages/scieasy-blocks-srs/src/scieasy_blocks_srs/preprocess/`. `SRSCalibrate` now performs the locked digitizer inversion, rejects already-calibrated inputs, and promotes `Image` collections to typed `SRSImage` outputs; the three downstream preprocess blocks now implement the spec-locked baseline, denoise, and normalization method families while preserving typed metadata. The SRS package exports and dependency declarations are updated, the skipped preprocess test stubs are replaced with concrete behavioral coverage, and `tests/plugins/test_phase11_skeleton.py` gains `test_srs_preprocess_impl_smoke()` so the workflow gate sees top-level SRS implementation coverage. (@Codex, 2026-04-08, branch: feat/issue-377/T-SRS-002-005-srs-preprocess, session: 20260408-071425-phase-11-srs-preprocess-chunk-t-srs-002)
- [#373] Add LCMS analysis core blocks `MetaboliteMatrix`, `MatrixPreprocess`, `UnivariateStats`, and `MultivariateAnalysis` for the metabolomics slice (@claude, 2026-04-08, branch: feat/issue-373/T-LCMS-013-016-analysis-core, session: 20260408-073039-t-lcms-013-016-lcms-analysis-core)

- [#376] T-IMG-024/T-IMG-025/T-IMG-026 imaging measurement bundle - replaces the remaining measurement skeleton bodies with concrete `RegionProps`, `PairwiseDistance`, and `Colocalization` implementations. `RegionProps` now computes `skimage.measure.regionprops_table` outputs from raster labels, keeps `label_id` first, supports optional intensity images, and concatenates collection inputs into a single `DataFrame` with `image_index`. `PairwiseDistance` now emits a long-format distance table for within-label object pairs using the live skeleton's `centroid` / `edge` metric contract plus optional `max_distance` filtering. `Colocalization` now computes Pearson, Manders, and Costes-style metrics for `channel_a` / `channel_b` image pairs with optional ROI masks and collection-aware pairing. The imaging plugin exports are updated, the measurement skip stubs are replaced with behavioral coverage in `packages/scieasy-blocks-imaging/tests/test_continuation_b_skeletons.py`, and `tests/plugins/test_phase11_skeleton.py` gains `test_imaging_measurement_impl_smoke()` so the Phase 11 workflow gate sees the landed implementation. (@Codex, 2026-04-08, branch: feat/issue-376/T-IMG-024-026-measurement, session: 20260408-071720-phase-11-imaging-measurement-chunk)
- [#363] LCMS foundation chunk 1 impl - replaces the T-LCMS-002 `get_types()` stub, the T-LCMS-003..006 LCMS IO skeleton bodies, and the T-LCMS-007 `ElMAVENBlock` / `AccuCorR` skeleton bodies with concrete implementations inside the Worker A foundation slice. `LoadMSRawFiles` now performs directory globbing plus lightweight mzML/mzXML header sniffing into typed `MSRawFile` metadata; `LoadPeakTable`, `LoadMIDTable`, and `LoadSampleMetadata` now load CSV/TSV/XLS(X) tables into typed LCMS `DataFrame` subclasses with cached pandas frames and spec-locked metadata; `SaveTable` now writes any `DataFrame` subclass to CSV/TSV/XLSX; `ElMAVENBlock` now classifies collected exports into peak vs MID tables and routes them through the concrete loaders; `AccuCorR` now resolves the bundled default R driver script, materialises table inputs to CSV for the subprocess contract, and wraps the returned corrected MID CSV as a typed `MIDTable`. The skipped LCMS types/IO/external tests are replaced with deterministic synthetic-fixture coverage, and `tests/plugins/test_phase11_skeleton.py` gains `test_lcms_foundation_chunk1_impl_smoke()` so the Phase 11 workflow gate sees the concrete implementation. (@claude, 2026-04-08, branch: feat/issue-363/lcms-foundation-a, session: 20260408-005849-phase-11-lcms-foundation-chunk-1-t-lcms)
- [#364] Sprint D SRS foundation chunk impl - replaces the T-SRS-001 `SRSImage` skeleton with a concrete `Image` subclass that requires `lambda/y/x`, adds a frozen `SRSImage.Meta` model with the nine locked SRS metadata fields, validates `wavenumbers_cm1` length against the lambda axis when shape is known, re-exports `SRSImage` from the package root, and enables `get_types()` to return `[SRSImage]`. The skipped SRS type stubs are replaced with concrete constructor / metadata / registry tests, and `tests/plugins/test_phase11_skeleton.py` gains `test_srs_types_impl_smoke()` so the workflow gate sees a top-level SRS implementation signal. (@claude, 2026-04-08, branch: feat/issue-364/T-SRS-001-srsimage-type, session: 20260408-005855-phase-11-srs-foundation-t-srs-001-srsima)
- [#372] T-IMG-017/T-IMG-018/T-IMG-020/T-IMG-021/T-IMG-022 segmentation core impl - replaces the remaining segmentation-core skeleton bodies with concrete imaging blocks on the `#372` branch. `Threshold` now supports all listed threshold modes and emits `Collection[Mask]`; `Watershed` now supports distance / gradient / markers modes with optional mask and markers inputs; `BlobDetect` now supports LoG / DoG / DoH and paints unique label disks across broadcast slices; `ConnectedComponents` now labels binary masks with validated connectivity; and the cleanup bundle now implements remove-small-objects, clear-border, fill-holes, expand-labels, and shrink-labels behavior for raster labels and masks. The imaging plugin exports are updated, the skipped package tests are replaced with behavioral coverage, and `tests/plugins/test_phase11_skeleton.py` gains `test_imaging_segmentation_core_impl_smoke()` so the workflow gate sees the concrete implementation. (@Codex, 2026-04-08, branch: feat/issue-372/T-IMG-017-022-seg-core, session: 20260408-045913-phase-11-imaging-segmentation-core-chunk)
- [#366] Sprint E LCMS isotope tracing core chunk - replaces the T-LCMS-008 `Calculate13CEnrichment`, T-LCMS-009 `FractionalLabeling`, T-LCMS-010 `CompareGroupMID`, T-LCMS-011 `FluxEstimate`, and T-LCMS-012 `PoolSizeNormalize` skeleton `NotImplementedError` bodies with concrete implementations in `packages/scieasy-blocks-lcms/src/scieasy_blocks_lcms/isotope_tracing/`. The chunk adds weighted tracer enrichment from long-format `MIDTable`s, `1 - M+0` fractional labeling, two-group isotopologue comparison with Bonferroni/FDR/none correction, the spec-scoped naive flux estimate (`labeling_rate * pool_size`, explicitly not full 13C-MFA), and `IS`/`TIC`/`median` peak-table normalization that preserves `PeakTable.meta`. It also replaces the five isotope-tracing skip stubs with synthetic-data tests and adds a package-local LCMS test-path shim so `pytest packages/scieasy-blocks-lcms/tests/` works before T-LCMS-020 packaging lands. (@claude, 2026-04-08, branch: feat/issue-366/T-LCMS-008-012-isotope-core, session: 20260408-010435-lcms-isotope-tracing-core-chunk-t-lcms-0)
- [#368] T-IMG-019 CellposeSegment impl - replaces the flagship cellpose skeleton with a real setup/process/teardown block on the imaging E2E critical path. `CellposeSegment` now loads the model once per run via `setup()`, forwards `diameter` / `flow_threshold` / `cellprob_threshold` / `channels` into `state.eval(...)`, emits `Collection[Label]` with integer raster masks and populated `Label.Meta.n_objects`, and clears CUDA cache during teardown when a GPU-backed state was used. The imaging package test suite adds focused coverage for the friendly missing-dependency path, custom-model validation, parameter forwarding, label serialization, and collection behavior, and `tests/plugins/test_phase11_skeleton.py` gains `test_imaging_cellpose_impl_smoke()` so the workflow gate sees the concrete implementation. (@Codex, 2026-04-08, branch: feat/issue-368/T-IMG-019-cellpose-segment, session: 20260408-011132-phase-11-imaging-cellpose-chunk)
- [#362] T-IMG-001 imaging types prerequisite impl - replaces the `scieasy_blocks_imaging.types` skeleton contract with concrete `Image`, `Mask`, `Label`, and `Transform` implementations. `Image.Meta` now uses typed `PhysicalQuantity` and `ChannelInfo` metadata with JSON round-trip and worker reconstruction coverage; `Mask` enforces boolean dtype; `Label` validates that at least one of `raster` or `polygons` is present and exposes the expected slots contract; `Transform` validates affine-matrix shapes and typed metadata. Imaging smoke coverage moved from the top-level skeleton placeholder into real package tests, and compatibility assertions in the geometry and axis-split suites were updated so already-landed imaging blocks validate against the new typed metadata contract. (@Codex, 2026-04-08, branch: feat/issue-362/T-IMG-001-imaging-types, session: 20260408-004852-phase-11-imaging-types-implementation-ch)
- [#360] Sprint C imaging morphology chunk impl - replaces the T-IMG-012 `MorphologyOp`, T-IMG-013 `EdgeDetect`, T-IMG-014 `RidgeFilter`, T-IMG-015 `Sharpen`, and T-IMG-016 `FFTFilter` skeleton `NotImplementedError` bodies with concrete slice-wise implementations. `MorphologyOp` now supports `erode` / `dilate` / `open` / `close` / `tophat` / `bottomhat` using configurable disk/square/cross footprints; `EdgeDetect` implements `sobel` / `scharr` / `prewitt` / `canny` with threshold validation; `RidgeFilter` wires `frangi` / `meijering` / `sato` / `hessian` with sigma-range construction; `Sharpen` adds `unsharp` and `laplacian`; `FFTFilter` adds low/high/band-pass filtering with cutoff validation and real-valued reconstruction. All five blocks broadcast over higher-rank inputs via `iterate_over_axes`, morphology package exports are wired into the plugin surface, the skipped per-block tests are replaced with concrete behavioral coverage, and `tests/plugins/test_phase11_skeleton.py` gains `test_imaging_morphology_impl_smoke()` so the workflow gate can detect the real implementation. (@claude, 2026-04-08, branch: feat/sprint-c/imaging-morphology, session: 20260408-002131-phase-11-imaging-morphology-chunk)
- [#358] Sprint C imaging preprocess subset B impl - replaces the T-IMG-008 `geometry.py`, T-IMG-009 `ConvertDType`, and T-IMG-010 `AxisSplit`/`AxisMerge` skeleton `NotImplementedError` bodies with narrow-scope implementations. Geometry: `Rotate`, `Flip`, `Crop`, `Pad`, and `Resize` now operate on `Image` inputs, use explicit ProcessBlock ClassVars, broadcast per-slice 2D work via `iterate_over_axes`, accept mask-driven crop boxes, and update `meta.pixel_size` proportionally on resize while preserving metadata/user lineage through the standard derived-object path. ConvertDType: supports `uint8`/`uint16`/`float32`/`float64`/`bool` targets with explicit `linear` and `clip` modes plus zero-threshold bool conversion. AxisSplit/AxisMerge: provide typed Collection-based split/merge over `t`/`z`/`c`/`lambda`, validate axis/order/shape consistency, append `meta.source_file` with `__axis=index`, and partition channel/wavelength metadata on split. Plugin tests replace the skipped stubs with deterministic behavioral coverage for the three files, and `tests/plugins/test_phase11_skeleton.py` gains `test_imaging_preprocess_b_impl_smoke()` so the workflow gate can detect the concrete implementation. (@claude, 2026-04-08, branch: feat/sprint-c/imaging-preprocess-b, session: 20260407-233745-phase-11-imaging-preprocess-chunk-1)

- [#356] Sprint C imaging preprocess subset A impl — replaces the T-IMG-004 `Denoise`, T-IMG-005 `BackgroundSubtract`, T-IMG-006 `Normalize`, and T-IMG-007 `FlatFieldCorrect` skeleton `NotImplementedError` bodies with narrow-scope implementations. Denoise pilot: `gaussian` + `median` via `skimage.filters`; `bilateral`/`nlmeans`/`wavelet` remain in the enum and raise `NotImplementedError`. BackgroundSubtract: all four methods (`rollingball` via `skimage.restoration.rolling_ball`, `tophat` via `skimage.morphology.white_tophat`, `polynomial` via numpy lstsq, `constant`) with `clip_to_zero` clamping and `radius`/`degree` validation. Normalize pilot: `minmax` / `zscore` / `percentile` with zero-variance safe handling and `per_slice=True|False` semantics (`histogram_match` enum value raises `NotImplementedError` pending a reference-image input port). FlatFieldCorrect: Tier 2 multi-input `run()` pairs the image Collection against the first `flat_field` (and optional `dark_frame`) item, applies `(image - dark) / (flat - dark) * mean(flat - dark)` with zero-denominator guard and `(y, x)` shape-mismatch validation; `BaSiC` method raises a friendly `ImportError` when `basicpy` is missing. All four blocks use `iterate_over_axes` to broadcast over extra `(t, z, c, lambda)` axes. Tests replace the 15 `pytest.skip` stubs with 39 real behavioural assertions (2-D happy paths, 5-D broadcasting, invalid-method / invalid-param `ValueError`s, deferred-method `NotImplementedError`s, zero-variance/zero-flat edge cases, polynomial linear-gradient exactness), gated on `pytest.importorskip("skimage")` where needed. `tests/plugins/test_phase11_skeleton.py` gains `test_imaging_preprocess_a_impl_smoke()` exercising one minimal call per block so the Verify Workflow Compliance gate recognises the impl PR. Broader methods and reference-image plumbing remain deferred and are recorded as **Status** notes under the T-IMG-004..007 spec sections. (@claude, 2026-04-08, branch: feat/sprint-c/imaging-preprocess-a-impl, session: 20260407-225426-sprint-c-imaging-preprocess-a-impl-t-img)
- [#354] Sprint C imaging IO pilot impl — replaces the T-IMG-002 (`LoadImage`) and T-IMG-003 (`SaveImage`) skeleton `NotImplementedError` bodies with narrow-scope implementations supporting `.tif`/`.tiff` via `tifffile` and `.zarr` via `zarr`. LoadImage detects axes from tiff series metadata (`T/Z/C/S/Y/X` → lowercase alphabet) with `ndim`-based defaults and an optional `axes` string override; auto-detects TIFF vs Zarr from extension; raises `FileNotFoundError` on missing path and `ValueError` on unsupported extension or mismatched axis length. SaveImage accepts a bare `Image` or a length-1 `Collection[Image]`, resolves format from extension or explicit `config['format']`, writes TIFF with axis metadata and Zarr as a group containing a `data` array plus an `axes` attribute, and creates parent directories automatically. Plugin test suite replaces the skipped stubs with real `tmp_path`-based round-trip tests (TIFF 2-D, TIFF 3-D C/Y/X, Zarr 3-D, axes-override happy path + mismatch, unsupported extension, missing path, empty collection, length>1 rejection, format override, parent-dir creation, bad format) and `tests/plugins/test_phase11_skeleton.py` gains a top-level `test_imaging_io_impl_smoke()` round-trip so the Verify Workflow Compliance gate recognises the impl PR. Broader format support (PNG/JPG/NPY/OME-TIFF/CZI/ND2/LIF, lazy `storage_ref` paths, indexed directory writes, multi-page TIFF) remains deferred and is noted under the T-IMG-002/003 spec sections. (@claude, 2026-04-08, branch: feat/sprint-c/imaging-io-impl, session: 20260407-223619-sprint-c-imaging-io-impl-t-img-002-003)
- [#351] Sprint C imaging skeleton continuation B — upgrades 17 placeholder modules from PR #346/#309 into proper API skeletons for T-IMG-021..037 per `docs/specs/phase11-imaging-block-spec.md` §9. ProcessBlock subclasses: ConnectedComponents (T-IMG-021), Cleanup bundle (T-IMG-022: RemoveSmallObjects/RemoveBorderObjects/FillHoles/ExpandLabels/ShrinkLabels), TrackObjects Phase-12 placeholder (T-IMG-023), RegionProps (T-IMG-024), PairwiseDistance (T-IMG-025), Colocalization (T-IMG-026), ComputeRegistration/ApplyTransform/RegisterSeries (T-IMG-027..029), AxisProjection+SelectSlice (T-IMG-030), scalar math bundle AddScalar/SubtractScalar/MultiplyScalar/DivideScalar (T-IMG-031), 2-port FIXED ImageCalculator (T-IMG-032), visualization bundle RenderPseudoColor/RenderOverlay/RenderMontage/RenderMovie/RenderHistogram (T-IMG-033). AppBlock subclasses: FijiBlock with Windows default path (T-IMG-034), NapariBlock (T-IMG-035), CellProfilerBlock (T-IMG-036), QuPathBlock (T-IMG-037). All bodies raise `NotImplementedError("T-IMG-NNN ... — impl pending (skeleton continuation B)")`. ClassVar annotations mandatory (type_name, name, description, version, category, algorithm, input_ports, output_ports, config_schema). Test scaffolding at `packages/scieasy-blocks-imaging/tests/test_continuation_b_skeletons.py` with class-existence assertions plus per-ticket `pytest.skip` behavioural stubs; top-level `tests/plugins/test_phase11_skeleton.py` extended with parametrized `test_continuation_b_modules_importable`; imaging plugin conftest gains a `sys.path` shim mirroring the top-level skeleton-test convention so tests run before T-IMG-038 packaging lands. NO touches to `pyproject.toml`, `types.py`, T-IMG-002..020, core, frontend, or other plugins. (@claude, 2026-04-07, branch: feat/issue-351/sprint-c-imaging-continuation-b, session: 20260407-215203-sprint-c-imaging-continuation-b-t-img-02)
- [#350] Sprint C imaging plugin skeleton continuation A — upgrades the 19 PR #309 placeholder files for T-IMG-002 through T-IMG-020 from one-line stubs into full skeleton classes with ClassVars (`type_name`, `name`, `description`, `category`, `algorithm`, `input_ports`, `output_ports`, `config_schema`) and typed method signatures (`process_item` / `load` / `save` / `setup` / `teardown`). Bodies raise `NotImplementedError("T-IMG-NNN ... — impl pending (skeleton continuation A)")` referencing the spec section. Covers IO (`LoadImage`, `SaveImage`), preprocess (`Denoise`, `BackgroundSubtract`, `Normalize`, `FlatFieldCorrect`, geometry bundle of 5 classes, `ConvertDType`, `AxisSplit`/`AxisMerge`, `Deconvolve` Phase-12 placeholder), morphology (`MorphologyOp`, `EdgeDetect`, `RidgeFilter`, `Sharpen`, `FFTFilter`), and segmentation (`Threshold`, `Watershed`, `CellposeSegment` FLAGSHIP with `setup`/`teardown` lifecycle, `BlobDetect`). 19 test stub files added with parametrized `pytest.skip` stubs and import + ClassVar smoke tests. `tests/plugins/test_phase11_skeleton.py` extended with `test_continuation_a_modules_importable` parametrized over the 19 modules. Pre-flight: pytest 36 passed / 80 skipped, ruff check + format clean, mypy clean. (@claude, 2026-04-07, branch: feat/issue-350/sprint-c-imaging-continuation-a, session: 20260407-214549-sprint-c-imaging-continuation-a-t-img-00)
- [#345] Sprint E LC-MS plugin skeleton — upgrades the minimal placeholder files from PR #309 into rich API skeletons for T-LCMS-002..019 per `docs/specs/phase11-lcms-block-spec.md` §9. 23 source classes with full ClassVars (input/output ports, config_schema, type_name, name, category, description), 4 Pydantic Meta shapes for the LC-MS types (`MSRawFile`, `PeakTable`, `MIDTable`, `SampleMetadata`), new `_LCMSBlockMixin` shared base, and 23 `pytest.skip` test stubs. All bodies `raise NotImplementedError("T-LCMS-NNN ... — impl pending (skeleton @ c08a885)")`. IO blocks subclass `IOBlock` (LoadMSRawFiles, LoadPeakTable, LoadMIDTable, LoadSampleMetadata, SaveTable). External tools: `ElMAVENBlock` and `GraphPadBlock` subclass `AppBlock`; `AccuCorR` subclasses `CodeBlock(language="r", mode="script")`. Isotope tracing (plugin USP): `Calculate13CEnrichment`, `FractionalLabeling`, `CompareGroupMID`, `FluxEstimate` (with explicit "NOT a 13C-MFA replacement" disclaimer per master plan §2.4), `PoolSizeNormalize`. Metabolomics analysis: `MetaboliteMatrix`, `MatrixPreprocess`, `UnivariateStats`, `MultivariateAnalysis`, `PathwayEnrichment` (KEGG REST Python-native default per spec §8 Q-6), `ConsumptionSecretionAnalysis`. Locked-decision audit verified: NO `MSSpectrum`/`MSRun`, NO MZmine/XCMS, NO removed isotope blocks, NO `SaveMIDTable`, NO QC blocks, NO `ManualReview`, NO full 13C-MFA. `pyproject.toml` not modified — T-LCMS-021 / impl agent owns entry-point registration. T-LCMS-001 scaffold from PR #309 preserved. Smoke import verified locally — all 23 symbols import with correct MRO. (@claude, 2026-04-07, branch: feat/issue-345/sprint-e-lcms-skeleton, session: 20260407-205130-sprint-e-lc-ms-plugin-skeleton-17-19-t-l)
- [#349] Sprint D SRS plugin skeleton — 11 T-SRS API contracts + test stubs (T-SRS-001 SRSImage type + T-SRS-002..012 blocks: SRSCalibrate, SRSBaseline, SRSDenoise, SRSNormalize, SRSVCA, SRSUnmix, SRSPCA, SRSICA, SRSKMeansCluster, ExtractSpectrum, BandRatio). Each block declares full ClassVars (name/description/version/category/algorithm/input_ports/output_ports/config_schema), typed method signatures with NotImplementedError bodies referencing the ticket ID, and ALLOWED_METHODS module constants where the spec locks an enum. Cross-plugin imports use scieasy_blocks_imaging.types per standards doc Q5; conftest imaging_types fixture from PR #309 is reused. 115 test stubs (all `pytest.skip` with per-ticket reason) seeded for the impl agent. NO RamanSpectrum, NO ALS baseline, NO SRSDespike/SRSWavenumberCalibrate/SRSMCR_ALS/SRSNMF/PeakFind/PeakFit per master plan §2.4. (@claude, 2026-04-08, branch: feat/sprint-d/srs-skeleton, session: 20260407-210551-sprint-d-srs-plugin-skeleton-11-t-srs-ap)
- [#344] Sprint C Track 2 imaging plugin skeleton — partial: T-IMG-001 imaging types module (`Image`/`Mask`/`Label`/`Transform`) upgraded from PR #309 placeholder to full Array/CompositeData subclasses with ClassVars (`required_axes`, `allowed_axes`, `canonical_order`), frozen Pydantic `Meta` models, and skeleton dtype/slot validators that raise `NotImplementedError` referencing T-IMG-001 for the impl agent. New `tests/test_types.py` with 22 `pytest.skip("T-IMG-001 impl pending")` test stubs mirroring the spec acceptance criteria. `__init__.py` exports `get_types()` returning the four classes plus a stub `get_blocks()`. T-IMG-002..T-IMG-037 placeholder files from PR #309 remain pending — this is a partial bundled-skeleton PR; continuation skeleton agent or impl agent must upgrade them. Workflow gate stages 1-3 complete; gate stages 4-6 pending PR creation. (@claude, 2026-04-07, branch: feat/issue-344/sprint-c-imaging-skeleton, session: 20260407-204753-sprint-c-imaging-plugin-skeleton-38-t-im)

- [#341] Add CodeBlock R runner integration audit tests (T-TRK-013 v2) at `tests/blocks/code/test_codeblock_r_integration.py` — two `@pytest.mark.requires_r`-gated integration tests that exercise the real `RRunner` subprocess path end-to-end per `docs/specs/phase11-implementation-standards.md` §9.1 T-TRK-013 §h. `test_codeblock_r_dataframe_roundtrip` passes a real `pandas.DataFrame` (strict spec contract — no dict-of-columns workaround per master plan §5 red flag #4 scope-argument shrinkage) through an inline R filter script and asserts the two `value > 5` rows come back. `test_codeblock_r_error_propagates` calls R `stop("scieasy-r-audit: boom from R")` and asserts the message is preserved in the surfaced Python `RuntimeError`. Both tests are additionally guarded by a `shutil.which("Rscript")` skip so CI (which has no R toolchain) stays green; pandas is imported via `pytest.importorskip` so collection works in environments without pandas. Registers the new `requires_r` pytest marker in `pyproject.toml`. Audit finding: the RRunner JSON bridge cannot serialise `pandas.DataFrame` (`json.dumps(namespace, default=str)` stringifies it) — filed as follow-up issue #342; the failing test is intentionally preserved as the audit signal and will be fixed in a separate PR. (@claude, 2026-04-08, branch: feat/issue-341/T-TRK-013-v2-r-runner-audit, session: 20260407-202243-t-trk-013-v2-codeblock-r-runner-audit-st)

- [#333] T-TRK-015 AppBlock + Fiji integration audit: four marker-guarded (`requires_fiji`) tests under `tests/blocks/app/test_appblock_fiji_integration.py` that launch the local Fiji install through `AppBlock` with a headless Gaussian-blur macro fixture (`tests/blocks/app/fixtures/headless_macro.ijm`) and audit launch/collect, FileWatcher cleanup, subprocess reaping, and exchange-dir cleanup. Registered the `requires_fiji` pytest marker in root `pyproject.toml` (T-IMG-038 will migrate it to `packages/scieasy-blocks-imaging/pyproject.toml`). Two audit findings filed as follow-up issues and recorded as `xfail(strict=False)`: #338 (AppBlock does not reap the subprocess after `run()` returns) and #339 (tempfile exchange dir not removed on successful exit). Audit-only — no changes to `src/scieasy/blocks/app/`. (@claude, 2026-04-07, branch: feat/issue-333/trk-015-appblock-fiji-audit, session: 20260407-200505-t-trk-015-appblock-fiji-integration-audi)
- [#334] T-TRK-014: `tests/blocks/code/test_codeblock_python_integration.py` — two integration tests driving `CodeBlock(language="python")` end-to-end against real storage backends. `test_codeblock_python_skimage_workflow` persists the T-TRK-003 segmentation images through `ZarrBackend`, runs a gaussian + Otsu threshold pipeline inside the inline script, and asserts a length-2 `Collection[Array]` repack. `test_codeblock_python_collection_unpack` persists three DataFrames via `ArrowBackend`, derives a `sum` column per item inside the script, and asserts a length-3 `Collection[DataFrame]` repack. Both hit production `LazyList -> view().to_memory()` and `_repack_outputs` paths with no mocks. Module-level `TEST_IMAGES_DIR.exists()` guard + `pytest.importorskip("skimage"/"tifffile"/"pyarrow")` per spec Q6 so environments without optional inputs skip cleanly. No `src/scieasy/blocks/code/` changes — auto-unpack/repack works correctly on both multi-item paths. Per `docs/specs/phase11-implementation-standards.md` §T-TRK-014. (@claude, 2026-04-07, branch: feat/issue-334/codeblock-python-integration-test, session: 20260407-200617-t-trk-014-codeblock-python-integration-t)

- [#331] T-TRK-012: `FilterCollection` block accepts a new `expression: str` config param parsed through an AST-whitelisted evaluator (new module `src/scieasy/blocks/process/builtins/expression_evaluator.py`). Expressions run against a per-item scope of `item`, `index`, `meta`, `framework`, `user`, plus a single whitelisted `len` call — no `eval()`, no trusted-mode bypass, no DSL. Forbidden constructs (arbitrary calls, imports, lambdas, dunder attribute/name access, non-literal subscripts, `BinOp`, `NamedExpr`) raise `ValueError("Expression contains forbidden construct: ...")` at parse time so workflow validation catches the error before the block runs. Legacy `predicate_key` / `predicate_value` equality match is preserved for backward compatibility and is mutually exclusive with `expression`. Tests: 18 unit tests for the evaluator (8 positive, 10 rejection) + 12 integration tests through `FilterCollection` including empty-Collection pass-through, dunder rejection, legacy path, and mutually-exclusive mode enforcement. Spec: `docs/specs/phase11-implementation-standards.md` §T-TRK-012 lines 1800-1889. (@claude, 2026-04-07, branch: feat/issue-331/filter-collection-expression, session: 20260407-200431-t-trk-012-filtercollection-ast-whitelist)
- [#326] Frontend `BlockNode.tsx` consumes the ADR-028 Addendum 1 dynamic-port mechanism: replaced the three pre-ADR-028 hardcoded `blockType === "io_block"` checks with property-based discriminators (`data.category === "io"` for the Browse button on `path` config fields per Addendum 1 §C11; `data.category === "io"` for hiding the `direction` config field per Addendum 1 §B fix #2; `data.schema?.direction` instead of `data.config?.direction` for the file-vs-directory branch per Addendum 1 §C8). Added `frontend/src/utils/computeEffectivePorts.ts` — a pure-function helper that consumes the new `BlockSchemaResponse.dynamic_ports` descriptor + the current driving config value and returns per-instance effective ports so port colours auto-update via `resolveTypeColor()` as the user changes the dropdown (no backend round-trip; static blocks pay zero allocation cost via reference identity). Wired the helper into the input/output Handle render paths in `BlockNode.tsx` per spec §d step 4. Extended `frontend/src/types/api.ts` `BlockSchemaResponse` interface with `dynamic_ports?: DynamicPortsConfig | null` and `direction?: string | null` (mirroring the backend Pydantic fields T-TRK-006 added in PR #321) and defined the `DynamicPortsConfig` interface with strict types (`Record<string, Record<string, string[]>>`, no `any`). Test coverage: 24 vitest unit tests in `frontend/src/utils/__tests__/computeEffectivePorts.test.ts` covering null/undefined static fallthrough, all six core enum values for both LoadData (`output_port_mapping`) and SaveData (`input_port_mapping`), mismatched-kind fallback, unknown-enum fallback, and multi-port selective override. 16 vitest tests in the new `frontend/src/components/nodes/BlockNode.test.tsx` covering all three discriminator behaviors (Browse button gating on category, hidden `direction` field on category, file-vs-directory branch reading `schema.direction`) plus the dynamic-port live-update render path (Array → DataFrame → Series, plus a static-block control). Per `docs/specs/phase11-implementation-standards.md` T-TRK-009 (Sprint A Level B PR 5 of 5 — last code-bearing PR in the sub-1b stack; T-TRK-010 doc-only follows). (@claude, 2026-04-07, branch: feat/issue-326/T-TRK-009-frontend-block-node-dynamic-ports, session: 20260407-192135-t-trk-009-frontend-blocknode-dynamic-por)

- [#323] Add `SaveData` block at `src/scieasy/blocks/io/savers/save_data.py` — the canonical core IO **output** block per ADR-028 Addendum 1 §C5/§C9. `SaveData(IOBlock)` carries `direction="output"`, `type_name="save_data"`, `category="io"`, a single `data` input port whose `accepted_types` is narrowed per-instance via `get_effective_input_ports()` against the `core_type` config enum, and a `dynamic_ports` descriptor that uses `input_port_mapping` (the symmetric mirror of LoadData's `output_port_mapping`). `SaveData.save()` dispatches to one of six private module-level functions per ADR-028 Addendum 1 §C9 (`_save_array`, `_save_dataframe`, `_save_series`, `_save_text`, `_save_artifact`, `_save_composite_data`) which absorb the write logic from the T-TRK-004-deleted `csv_adapter` / `parquet_adapter` / `zarr_adapter` / `generic_adapter`. Supported extensions: Array → `.npy` / `.npz` / `.zarr` / `.parquet` (1-D); DataFrame → `.csv` / `.tsv` / `.parquet` / `.json`; Series → `.csv` / `.tsv` / `.parquet` / `.json` (single-column); Text → `.txt` / `.md` / `.html` / `.xml` / `.log` / `.yaml` / `.toml` / `.json`; Artifact → raw bytes plus a `.meta.json` sidecar; CompositeData → JSON manifest plus per-slot sidecar files in a `<stem>_slots/` subdirectory. `allow_pickle=True` opt-in enables `.pkl` / `.pickle` writes for Array / DataFrame / Series with an explicit WARNING-level security log via `logging`. `SaveData.load()` raises `NotImplementedError` (output-only). Mixed-type Collections, multi-item Collections, empty Collections, and wrong-type-for-core-type inputs all raise `ValueError` per spec §j out-of-scope rule. The new `src/scieasy/blocks/io/savers/__init__.py` re-exports `SaveData` and `src/scieasy/blocks/io/__init__.py` adds it to `__all__`. Registered as `save_data = "scieasy.blocks.io.savers.save_data:SaveData"` under `[project.entry-points."scieasy.blocks"]` so it surfaces in the block palette. Test coverage: 53 new tests in `tests/blocks/io/test_save_data.py` covering class-shape invariants, parametrized `get_effective_input_ports`, end-to-end round-trip per core type via the underlying lib (pyarrow / numpy / json / pickle / zarr — independent of T-TRK-007 LoadData merge order), allow_pickle gate (default-rejects + opt-in writes + WARNING log), mixed-type Collection rejection, and the `SaveData.load() → NotImplementedError` contract. Per `docs/specs/phase11-implementation-standards.md` T-TRK-008 (Sprint A Level B PR 4 of 5, parallel with T-TRK-007). (@claude, 2026-04-07, branch: feat/issue-323/T-TRK-008-save-data-class, session: 20260407-185152-t-trk-008-savedata-class-6-private-save)
- [#323] Extend `BlockRegistry._validate_dynamic_ports` (`src/scieasy/blocks/registry.py`) to accept either `output_port_mapping` (LoadData / input-direction blocks) **or** `input_port_mapping` (SaveData / output-direction blocks). Exactly one of the two keys must be present per ADR-028 Addendum 1 §C5; declaring both raises `ValueError`. T-TRK-006 (PR #321) only declared the `output_port_mapping` branch because LoadData was the first dynamic-ports consumer; SaveData is the symmetric output-direction counterpart and must use `input_port_mapping` per spec line 1497 and the T-TRK-008 prompt's locked autonomy limit. Error-message paths now interpolate the actual mapping key so diagnostics still pinpoint the offending field. The 22 existing dynamic-ports tests in `tests/blocks/test_dynamic_ports.py` continue to pass without modification. Per `docs/specs/phase11-implementation-standards.md` T-TRK-008. (@claude, 2026-04-07, branch: feat/issue-323/T-TRK-008-save-data-class, session: 20260407-185152-t-trk-008-savedata-class-6-private-save)

- [#322] Add `LoadData` class (`src/scieasy/blocks/io/loaders/load_data.py`) plus six private module-level dispatch functions (`_load_array`, `_load_dataframe`, `_load_series`, `_load_text`, `_load_artifact`, `_load_composite_data`). `LoadData` is the canonical core IO loader: a single dynamic-port block whose `data` `OutputPort` accepted-types are computed per-instance from the `core_type` enum config field via `get_effective_output_ports()`. The hardcoded `_CORE_TYPE_MAP` covers Array, DataFrame, Series, Text, Artifact, and CompositeData. Format coverage: `.npy` / `.npz` / `.zarr` / single-column `.parquet` (Array); `.csv` / `.tsv` / `.parquet` / `.json` (DataFrame); single-column `.csv` / `.tsv` / `.parquet` (Series); `.txt` / `.md` / `.html` / `.xml` / `.log` / `.yaml` / `.yml` / `.toml` (Text); any file with optional `.meta.json` sidecar (Artifact); JSON manifest pointing at heterogeneous slot files (CompositeData). Pickle is gated via `allow_pickle=False` default — `.pkl` / `.pickle` paths raise `ValueError` with a security message unless explicitly opted in, in which case a WARNING is logged before loading. The class body, `_CORE_TYPE_MAP`, `dynamic_ports` ClassVar, and `config_schema` are copied verbatim from the standards doc skeleton (ADR-028 Addendum 1 §C5/§C9, `docs/specs/phase11-implementation-standards.md` T-TRK-007 lines 1292-1413). The six private functions absorb logic from the deleted `csv_adapter` / `parquet_adapter` / `zarr_adapter` / `generic_adapter` modules (T-TRK-004 / PR #319). `LoadData` is re-exported from `scieasy.blocks.io` and registered under the `[project.entry-points."scieasy.blocks"]` group as `load_data`. 52 tests in `tests/blocks/io/test_load_data.py` cover instantiation, dynamic-port contract, format-specific round-trips, `allow_pickle` gating, and framework `run()` dispatch. (@claude, 2026-04-07, branch: feat/issue-322/T-TRK-007-load-data-class, session: 20260407-184853-t-trk-007-loaddata-class-6-private-load)
- [#320] Add `Block.get_effective_input_ports()` and `Block.get_effective_output_ports()` instance methods on the `Block` ABC plus a new `dynamic_ports: ClassVar[dict[str, Any] | None]` declarative descriptor (default `None`). The two effective-ports methods default to a defensive copy of the class-level `input_ports` / `output_ports` ClassVars; dynamic blocks (e.g. the upcoming `LoadData` / `SaveData` from T-TRK-007 / T-TRK-008) override them to return per-instance ports computed from `self.config`. Per ADR-028 Addendum 1 D1, D2 and `docs/specs/phase11-implementation-standards.md` T-TRK-006 (Sprint A Level B PR 2 of 5, bundled with the framework read-path migration per master plan §4.3 Q3). The ClassVar carries a contract docstring describing the enum-only `{source_config_key, output_port_mapping: {port_name: {enum_value: [type_names]}}}` shape. (@claude, 2026-04-07, branch: feat/issue-320/T-TRK-006-block-abc-dynamic-port-hooks, session: 20260407-182146-t-trk-006-block-abc-dynamic-port-hooks-f)
- [#320] Extend `BlockSpec` (`src/scieasy/blocks/registry.py`) with two new dataclass fields: `direction: str = ""` (populated from the class-level `direction` ClassVar — `"input"` / `"output"` for IO blocks, empty string otherwise) and `dynamic_ports: dict[str, Any] | None = None` (the enum-driven dynamic-port descriptor copied verbatim from the class). `_spec_from_class` populates both fields via `getattr(cls, ...)`. Per ADR-028 Addendum 1 D3 and T-TRK-006. (@claude, 2026-04-07, branch: feat/issue-320/T-TRK-006-block-abc-dynamic-port-hooks, session: 20260407-182146-t-trk-006-block-abc-dynamic-port-hooks-f)
- [#320] Add `BlockRegistry._validate_dynamic_ports(cls)` static method that rejects malformed `dynamic_ports` dicts at scan time with descriptive `ValueError` messages. Validates: top-level dict shape, required `source_config_key` (non-empty string), required `output_port_mapping` (dict whose keys are non-empty port names, whose values are enum-value dicts mapping non-empty enum strings to non-empty lists of non-empty type-name strings). Called from `_spec_from_class` so every registered class is validated at import time. Nine negative test cases in `tests/blocks/test_dynamic_ports.py` cover the documented malformed shapes. Per ADR-028 Addendum 1 D3 and T-TRK-006. (@claude, 2026-04-07, branch: feat/issue-320/T-TRK-006-block-abc-dynamic-port-hooks, session: 20260407-182146-t-trk-006-block-abc-dynamic-port-hooks-f)
- [#320] Extend `BlockSchemaResponse` (`src/scieasy/api/schemas.py`) with two new optional Pydantic fields: `dynamic_ports: dict[str, Any] | None = None` (D4) and `direction: str | None = None` (D7). `api/routes/blocks.py` `get_block_schema` populates both from the registered `BlockSpec` (`spec.dynamic_ports` and `spec.direction or None` — the empty-string-to-None conversion keeps non-IO JSON clean). Decision: the new fields live on `BlockSchemaResponse` (per-block schema endpoint) rather than `BlockSummary` (palette listing) because the frontend reads `data.schema?.dynamic_ports` and `data.schema?.direction` per master plan §2.2. Per ADR-028 Addendum 1 D4 / D7 and T-TRK-006. (@claude, 2026-04-07, branch: feat/issue-320/T-TRK-006-block-abc-dynamic-port-hooks, session: 20260407-182146-t-trk-006-block-abc-dynamic-port-hooks-f)

### Changed

- [#328] Update `docs/architecture/ARCHITECTURE.md` (§5.3 IOBlock paragraph + §5.4 entry-point group table + §12 extension-points table + §12.1 callable protocol code block), `docs/architecture/PROJECT_TREE.md` (replace the `src/scieasy/blocks/io/adapters/` subtree + `adapter_registry.py` line with new `loaders/` and `savers/` subtrees containing `LoadData` and `SaveData`; drop the `[project.entry-points."scieasy.adapters"]` example; refresh the file count summary), and `docs/guides/block-sdk.md` (drop the `scieasy.adapters` row from the entry-point tables; rewrite §3.2 IOBlock subsection to reflect the abstract base class with a working concrete subclass example; add new §3.6 "Writing a dynamic-port block" with a worked example based on the live `LoadData` class body — `_CORE_TYPE_MAP` table, `dynamic_ports` ClassVar descriptor format, `get_effective_output_ports()` override, two-bad-alternatives motivation, and pointers to ADR-028 Addendum 1 §C5/§C9/§B + ADR-029 for variadic port count) to reflect the post-ADR-028 + Addendum 1 architecture (`IOBlock` ABC + `LoadData`/`SaveData` dynamic-port loaders, no separate `scieasy.adapters` entry-point group). Add a "Status (2026-04-08): SUPERSEDED by ADR-028 §D4" stamp at the top of `docs/adr/ADR.md` ADR-025 §6 (Adapter registration via entry-points); the §6 body is preserved verbatim per CLAUDE.md §10 historical-ADR immutability rule. This is the last ticket in the Sprint A sub-1b stacked cascade — once it lands, T-TRK-004/006/007/008/009/010 are all on main. Per `docs/specs/phase11-implementation-standards.md` T-TRK-010 (Sprint A Level B PR 6 of 6, last in stack). (@claude, 2026-04-07, branch: docs/issue-328/T-TRK-010-architecture-docs-update, session: 20260407-194536-t-trk-010-architecture-md-project-tree-m)
- [#323] `IOBlock.run()` output direction now wraps the path receipt in a single-item `Collection` of `Text` (`Collection(items=[Text(content=str(path))], item_type=Text)`) instead of returning a literal `dict[str, str]` under a `dict[str, Collection]` signature, removing the pre-existing `# type: ignore[dict-item]` suppression on `src/scieasy/blocks/io/io_block.py`. This resolves the bookkeeping debt noted in `project_phase11_ttrk007_008_bookkeeping.md` Item 1 and restores strict typing across the IO surface. Verified zero production consumers of the old shape via grep: no `src/scieasy` code reads `result["path"]` from `IOBlock.run()` (the key has always been a write receipt for downstream observers, not a load-bearing data path). Updated `test_output_direction_forwards_data_to_save` in `tests/blocks/io/test_io_block_abc.py` to assert the new typed shape (Collection of length 1, `item_type is Text`, `[0].content == path`) — strictly stronger than the old literal-dict equality assertion. Per `docs/specs/phase11-implementation-standards.md` T-TRK-008. (@claude, 2026-04-07, branch: feat/issue-323/T-TRK-008-save-data-class, session: 20260407-185152-t-trk-008-savedata-class-6-private-save)
- [#320] Migrate every framework callsite that reads `input_ports` / `output_ports` on a block instance to the new effective-ports methods: `Block.validate()` reads `self.get_effective_input_ports()`; `ProcessBlock.run()` reads `self.get_effective_output_ports()` for the Collection output name; `SubWorkflowBlock._sequential_execute()` reads `block.get_effective_input_ports()` for child input port enumeration (grep audit found this in addition to the spec's listed sites); `workflow/validator.py` instantiates a per-node block from spec + node config and uses effective ports for type-compatibility (Check 5) and dangling-port (Check 6) checks, with an explicit fallback to `spec.input_ports` / `spec.output_ports` when instantiation is impossible (test fixtures that inject a bare `BlockSpec` without registering an importable class). Class-introspection sites — `_spec_from_class`, `routes/blocks.py:_summary`, `routes/blocks.py:validate_connection_route`, `harness.py`, `ai/generation/validator.py`, the CLI block-package template — intentionally keep their class-level reads per the standards doc §h two introspection sites. Per ADR-028 Addendum 1 D5 / D6 and T-TRK-006. Grep audit verified zero `self.input_ports` / `self.output_ports` / `block.input_ports` / `block.output_ports` hits remain in `src/`. (@claude, 2026-04-07, branch: feat/issue-320/T-TRK-006-block-abc-dynamic-port-hooks, session: 20260407-182146-t-trk-006-block-abc-dynamic-port-hooks-f)

### Removed

- [#318] Delete the bundled `src/scieasy/blocks/io/adapters/` directory (10 files: `__init__.py`, `base.py`, plus 8 `*_adapter.py` modules), `src/scieasy/blocks/io/adapter_registry.py`, and the `[project.entry-points."scieasy.adapters"]` table in `pyproject.toml`. Per ADR-028 §D1/§D2/§D4 and Phase 11 master plan §2.5 sub-1b PR-A (bundled with PR-A-2 because rewriting `io_block.py` is unimplementable while `adapter_registry.py` still imports from `adapters/*`). The legacy adapter dispatch layer is replaced by plugin-owned `IOBlock` subclasses; the TIFF and mzXML adapter logic moves to `scieasy-blocks-imaging` and `scieasy-blocks-lcms` via T-IMG-002 and T-LCMS-002 respectively, sourced from this PR's git history. Concrete core loaders (`LoadData`/`SaveData`) arrive in T-TRK-007/T-TRK-008. Also delete the now-obsolete `tests/blocks/test_io_block.py`, `tests/blocks/test_adapters.py`, and `tests/blocks/test_adapter_registry.py` test files. (@claude, 2026-04-07, branch: refactor/issue-318/T-TRK-004-delete-adapters-rewrite-ioblock, session: 20260407-174142-t-trk-004-delete-adapters-directory-and)
- [#310] Delete `src/scieasy/blocks/process/contrib/` stub directory and its four 1-line placeholder files (`__init__.py`, `cellpose_segment.py`, `spectral_pca.py`, `baseline_correction.py`). Per Phase 11 master plan §2.5 sub-1a and `docs/specs/phase11-implementation-standards.md` T-TRK-001 (Sprint A Track 1 sub-1a parallel batch). The contrib pattern is being abandoned in favour of the plugin package pattern; the real Cellpose implementation lives in T-IMG-019 (Sprint C, `scieasy-blocks-imaging`). All four files were docstring-only stubs with zero business logic and zero test imports. `git grep "scieasy.blocks.process.contrib"` returns 0 hits in non-doc files; remaining hits inside `docs/specs/phase11-implementation-standards.md` are the standards-doc description of this very ticket and are out of scope for this PR. `pytest -x --no-cov tests/blocks/` green: 342 passed, 11 skipped. (@claude, 2026-04-07, branch: refactor/issue-310/T-TRK-001-delete-process-contrib, session: 20260407-165842-t-trk-001-delete-process-contrib-stub-di)

### Changed

- [#318] Rewrite `src/scieasy/blocks/io/io_block.py` as an abstract base class with `load()` / `save()` abstract methods and a default `run()` dispatch keyed on `direction`, per ADR-028 §D1 and `docs/specs/phase11-implementation-standards.md` T-TRK-004 (lines 914-976). Plugin IO blocks subclass `IOBlock` directly and register via the existing `scieasy.blocks` entry-point group. `name` and `description` ClassVars are preserved so the existing `BlockRegistry._scan_builtins` continues to surface the canonical `"IO Block"` / `"io_block"` identity that integration tests, workflow YAMLs, and the API connection-validator depend on. `IOBlock()` direct instantiation now raises `TypeError` per the abstractmethod contract — `tests/blocks/io/test_io_block_abc.py` asserts this. (@claude, 2026-04-07, branch: refactor/issue-318/T-TRK-004-delete-adapters-rewrite-ioblock, session: 20260407-174142-t-trk-004-delete-adapters-directory-and)
- [#318] Refactor `src/scieasy/api/runtime.py` and `src/scieasy/ai/generation/templates.py` to drop their imports of the deleted adapter layer. `runtime.py::upload_file` now builds `StorageReference` directly from the destination path; `runtime.py::preview_data` now reads TIFF previews via `import tifffile; tifffile.imread(path)` directly, mirroring the behaviour of the deleted `TIFFAdapter`. The `ai/generation/templates.py` IOBlock prompt no longer references the removed `scieasy.blocks.io.adapters` namespace. Both refactors are required by `docs/specs/phase11-implementation-standards.md` T-TRK-004 §h step 5 ("every importer must be deleted or refactored"). (@claude, 2026-04-07, branch: refactor/issue-318/T-TRK-004-delete-adapters-rewrite-ioblock, session: 20260407-174142-t-trk-004-delete-adapters-directory-and)

### Added

- [#308] Phase 11 skeleton: plugin scaffolding and placeholder stubs for all 89 tickets (`packages/scieasy-blocks-{imaging,srs,lcms}/`). Creates the directory layout, minimal `pyproject.toml`, `README.md`, and placeholder `.py` stubs for every block ticket listed in `docs/specs/phase11-implementation-standards.md` §9.2 / §9.3 / §9.4. Every source body is `raise NotImplementedError("T-XXX-NNN: ...")` — no business logic. 69 block stubs total: 37 imaging (T-IMG-001..T-IMG-037), 12 SRS (T-SRS-001..T-SRS-012), 20 LC-MS (T-LCMS-002..T-LCMS-019), plus 1 R script placeholder for `T-LCMS-007` (`external/scripts/accucor.R`). Subdirectory names follow the standards doc verbatim (`component_analysis/`, `spectral_extraction/`, `isotope_tracing/`, `analysis/`, `math/scalar_ops.py`, `interactive/cell_profiler_block.py`, `component_analysis/srs_kmeans.py`). Cross-plugin SRS `imaging_types` fixture (§Q5) lives in `packages/scieasy-blocks-srs/tests/conftest.py`. LC-MS marker registration (`requires_r` / `requires_elmaven` / `requires_graphpad`) lives in `packages/scieasy-blocks-lcms/tests/conftest.py`. Adds `tests/plugins/test_phase11_skeleton.py` — a repo-level smoke test that prepends each plugin's `src` dir to `sys.path` and parametrizes over every placeholder module (93 passing tests, including 3 representative `NotImplementedError` checks for `Image`/`SRSImage`/`MSRawFile`). Stub `__init__` signatures annotated `(*args: object, **kwargs: object) -> None` so `mypy --disallow-untyped-defs` is clean across all 90 plugin source files. Single monolithic PR covering all 4 tracks per §Q4 rationale (no business logic, easier completeness audit, avoids cross-package stacked-merge dependencies). Out of scope and explicitly deferred: Track 1 deletes (T-TRK-001/002/003/004), any block implementation, `pyproject.toml` entry_points, root `pyproject.toml` `testpaths` updates, and T-IMG-038 / T-SRS-013 / T-SRS-014 / T-SRS-000 / T-LCMS-001 / T-LCMS-020 / T-LCMS-021 meta-tickets (packaging + integration tests which land with their own PRs). Commit-as-you-go protocol per master plan §0.1: empty safety checkpoint, then one commit per plugin, then smoke test + type-annotation commit. `ruff check`, `ruff format --check`, plugin-scope `mypy`, plugin smoke test, and full `pytest --no-cov` (1879 passed, 14 skipped — no regressions vs `main`) all green locally before push. Bridges the design/spec phase (7 Phase 11 docs merged to `main` @ b0264bf) and the Sprint A-E implementation cascade (~80 downstream implementation agents, each owning a single file). (@claude, 2026-04-07, branch: feat/issue-308/phase11-skeleton-plugin-scaffolding, session: 20260407-162332-phase-11-skeleton-plugin-scaffolding-and)
- [#306] Phase 11 implementation standards (`docs/specs/phase11-implementation-standards.md`, ~3340 lines). Master task-decomposition document for the Phase 11 plugin cascade — single source of truth for the skeleton agent and the 88 implementation agents that follow. Closes #305 (the v1 task-decomposition agent died silently after posting a change plan; v2 succeeds via aggressive context budgeting and chunked commit-as-you-go protocol). Defines 89 tickets across 4 tracks: Track 1 (15 T-TRK-* core block cleanup + ADR-028 implementation tickets, full per-ticket format with 13 subsections each), Track 2 (38 T-IMG-* imaging tickets, compact reference pointers to `phase11-imaging-block-spec.md`), Track 3 (15 T-SRS-* SRS tickets, compact reference pointers to `phase11-srs-block-spec.md`), Track 4 (21 T-LCMS-* LC-MS tickets, compact reference pointers to `phase11-lcms-block-spec.md`). Walks the full Phase 10-style standards document structure: §1 Purpose, §2 Scope (in-scope tracks + explicit out-of-scope ADR-029 freeze, plugin packages beyond imaging/SRS/LCMS, ARCHITECTURE.md scope cap, frontend refactor cap), §3 Cross-reference table, §4 Dependency graph (ASCII DAG of Sprint A0-A5/B/C0-C3/D0-D2/E0-E3 with parallel/sequential edges), §5 Recommended chained PR order, §6 Universal rules (workflow gate mandatory, branch naming `feat/issue-N/T-XXX-NNN-short`, stacked PR base, scope discipline, all-checks-green requirement, CHANGELOG attribution, PR body cross-references, **8 scope-violation red flags verbatim from master plan §5** as the audit-agent checklist, doc-external-changes restriction, branch base from origin/main, one-ticket-per-PR rule), §7 Universal acceptance criteria (10-item checklist), §8 Open questions resolved (12 implementation-phase decisions: Q1 branch+ticket numbering scheme, Q2 sub-1c parallelism, **Q3 PR-B coupling resolution that bundles original PR-B-1 and PR-B-2 into T-TRK-006** because splitting them leaves the codebase broken intermediate, Q4 single skeleton PR strategy, Q5 cross-plugin import pattern via `pytest.importorskip` + `imaging_types` session fixture, Q6 E2E test image constants in `tests/fixtures/test_images.py`, Q7 nine new pytest markers `requires_cellpose/napari/fiji/cellprofiler/qupath/r/elmaven/graphpad/lcms`, Q8 marker registration locations split between root and per-plugin pyproject.toml, **Q9 ADR-028 §D3 supersession anti-drift flag** for any imaging-spec reference to the now-removed six-loader structure, Q10 SRS canonical block count is 11 not 8, **Q11 ImageCalculator 2-port-fixed in 0.1.0** with variadic deferred to ADR-029, Q12 monorepo `testpaths` discovery in root pyproject.toml), §9 Per-ticket sections (Track 1 full format ~1430 lines, Tracks 2/3/4 compact ~1000 lines combined), §10 Summary table (one row per ticket with track/sprint/diff/deps/coupling), §11 E2E test plan (Phase 1 post-Sprint-D imaging+SRS pipeline using 4 K562 test images per master plan §8, Phase 2 post-Sprint-E full plugin set, Chrome GUI E2E via `mcp__Claude_in_Chrome__*`), §12 References. Five coupled ticket bundles documented: T-TRK-004 (delete adapters + rewrite io_block.py — must be atomic), T-TRK-006 (Block ABC dynamic-port hooks + framework migration — bundles PR-B-1+PR-B-2 per Q3), T-IMG-008/010/022/030/031/033 (geometry/axis/cleanup/projection/scalar/visualization 5-block bundles per imaging spec author's coupling judgment), T-LCMS-006 (LoadSampleMetadata + SaveTable), T-LCMS-007 (ElMAVENBlock + AccuCorR — shared marker registration). Three flagged inconsistencies between source docs: (1) the master plan §2.4 "~8 SRS blocks" estimate vs the SRS spec's actual 11 implementation blocks (resolved Q10 — spec is canonical), (2) potential imaging-spec residual references to the original ADR-028 §D3 six-loader structure (resolved Q9 — addendum supersedes), (3) ImageCalculator variadic ambition vs the ADR-029 freeze (resolved Q11 — fixed 2-port in 0.1.0). Ticket ID T-TRK-005 deliberately reserved-no-implement so that ticket numbering remains stable across this document, the master plan, and future PR descriptions. Document was written in 7 chunked commits with `git push` after each chunk per the v2 commit-as-you-go protocol (lessons from v1 silent death) — chunk 1 placeholder, chunk 2 §1-§8, chunk 3 §9.1 Track 1, chunk 4 §9.2 Track 2, chunk 5 §9.3 Track 3, chunk 6 §9.4 Track 4, chunk 7 §10-§12, plus this CHANGELOG entry. No source/test/frontend changes — docs-only. Enables the Phase 11 skeleton agent + the 88 per-ticket implementation agents. (@claude, 2026-04-07, branch: docs/issue-306/phase11-implementation-standards-v2, session: 20260407-154306-phase-11-implementation-standards-task-d)
- [#301] Phase 11 LC-MS plugin block specification (`docs/specs/phase11-lcms-block-spec.md`, 3753 lines). Documentation-only spec authored per master plan §2.4 LC-MS PLUGIN lock. Defines the `scieasy-blocks-lcms` plugin package: 4 plain-DataFrame/Artifact types (`MSRawFile(Artifact)`, `PeakTable(DataFrame)`, `MIDTable(DataFrame)`, `SampleMetadata(DataFrame)` — **NO `MSSpectrum(Series)` or `MSRun(Array)`** per user), 19 blocks across 21 T-LCMS tickets: IO (5) `LoadMSRawFiles` / `LoadPeakTable` / `LoadMIDTable` / `LoadSampleMetadata` / `SaveTable`; external tools (2) `ElMAVENBlock` (AppBlock wrapping the user's ElMAVEN interactive desktop workflow per §8 Q-2 — no UI scripting, 30-min watch_timeout, FileWatcher picks up user exports) and `AccuCorR` (CodeBlock `language="r"` wrapping the AccuCor R package v0.3.1 via a bundled `accucor_default.R` driver script resolved via `importlib.resources` per §8 Q-1); isotope tracing USP cluster (5) `Calculate13CEnrichment` / `FractionalLabeling` / `CompareGroupMID` / `FluxEstimate` (simple `labeling_rate × pool_size` — explicitly NOT 13C-MFA per §8 Q-9) / `PoolSizeNormalize`; metabolomics analysis (6) `MetaboliteMatrix` / `MatrixPreprocess` / `UnivariateStats` / `MultivariateAnalysis` / `PathwayEnrichment` (Python-native KEGG REST default per §8 Q-6, MetaboAnalystR R wrapper deferred) / `ConsumptionSecretionAnalysis` (NEW per user, spent-vs-fresh media flux with optional cell count normalization per §8 Q-7); external plotting (1) `GraphPadBlock` (AppBlock wrapping GraphPad Prism, no default path per §8 Q-8, Windows-primary with non-Windows warning). Spec explicitly forbids adding scan-level data types, `EnumerateIsotopologues` / `MatchIsotopologues` / `ComputeMID` / `AtomPercentExcess` / `TimeCourseFit` / `TracerFateMap` (AccuCor does all of this externally), QC blocks, `ManualReview` blocks, MZmine / XCMS (user: "先实现 ElMAVEN"), and full 13C-MFA. MID long-format locked per §8 Q-3 (matches AccuCor output and the user's verbatim cytosine example). Sample-column detection heuristic per §8 Q-4 (`_KNOWN_IDENTITY_COLUMNS` ∪ `_KNOWN_ATOM_COLUMNS` exclusion with regex override). Multi-tracer support (`C13 + H2`) per §8 Q-5. Per §8 Q-11, no LC-MS block uses `dynamic_ports` — all four plugin types render via the existing `resolveTypeColor` walk with no frontend changes. 12 resolved open questions (§8 Q-1 through Q-12) capture every decision the spec agent owned beyond the master plan lock. 21 T-LCMS tickets each with the 12-subsection pattern matching `phase10-implementation-standards.md`: T-LCMS-001 scaffold, T-LCMS-002 types, T-LCMS-003..006 IO, T-LCMS-007 ElMAVEN+AccuCor, T-LCMS-008..012 isotope tracing (most-detailed cluster as the plugin USP — T-LCMS-008 `Calculate13CEnrichment` includes the Antoniewicz 2018 formula `E[c,s] = sum(n * M+n[c,s]) / n_max[c]` with the user's verbatim cytosine worked example), T-LCMS-013..018 metabolomics analysis, T-LCMS-019 GraphPad, T-LCMS-020 entry-point registration + smoke test, T-LCMS-021 isotope tracing integration test with synthetic fixtures (per master plan §8, LC-MS does NOT use the 4 imaging test images at Example/images/ — those are imaging/SRS only). Section §11 contains the full end-to-end integration test body chaining `LoadPeakTable` → `LoadMIDTable` → `LoadSampleMetadata` → `Calculate13CEnrichment` → `CompareGroupMID` → `MetaboliteMatrix` → `MatrixPreprocess` → `UnivariateStats` → `SaveTable` with no external-tool dependencies and no network calls. Package layout: `packages/scieasy-blocks-lcms/` with `pyproject.toml` declaring `scieasy.blocks` + `scieasy.types` entry-points, `scripts/accucor_default.R` as package data via `[tool.setuptools.package-data]`, and `requires_elmaven` / `requires_r` / `requires_graphpad` pytest markers for external-tool integration tests. Estimated total implementation size ~9,680 LOC across 21 stacked PRs. No ADR changes, no source/test changes — docs-only. (@claude, 2026-04-07, branch: docs/issue-301/phase11-lcms-spec, session: 20260407-011856-phase-11-lc-ms-plugin-block-specificatio)
- [#298] Phase 11 imaging plugin specification (`docs/specs/phase11-imaging-block-spec.md`, ~6200 lines). Documentation-only spec that codifies the contract for the `scieasy-blocks-imaging` plugin package and its 38 implementation tickets (T-IMG-001 through T-IMG-038), mirroring the structure of `docs/specs/phase10-implementation-standards.md`. Defines the four imaging types (`Image(Array)` 2D-to-6D with the t/z/c/lambda/y/x alphabet, `Mask(Image)` strict bool-dtype, `Label(CompositeData)` with raster+polygon slots, `Transform(Array)` for affine matrices), the unified `LoadImage`/`SaveImage` IO blocks (TIFF/PNG/JPG/NPY/Zarr first-class plus optional CZI/ND2/LIF extras, lazy loading at 128 MiB threshold, `SCIEASY:`-prefixed JSON-in-ImageDescription metadata protocol moved verbatim from OptEasy `tiff_adapter.py`), the flagship `CellposeSegment` block using `ProcessBlock.setup`/`teardown` to load the cellpose model ONCE per run (Q-IMG-2 CPU default, GPU auto-fallback, `torch.cuda.empty_cache()` in teardown), 12 preprocessing blocks (`Denoise`/`BackgroundSubtract`/`Normalize`/`FlatFieldCorrect`/`Rotate`/`Flip`/`Crop`/`Pad`/`Resize`/`ConvertDType`/`AxisSplit`/`AxisMerge` plus `Deconvolve` Phase 12 placeholder), 5 morphology blocks (`MorphologyOp`/`EdgeDetect`/`RidgeFilter`/`Sharpen`/`FFTFilter`), 8 segmentation blocks (`Threshold` with 8 methods, `Watershed`, `BlobDetect`, `ConnectedComponents`, the 5-block cleanup bundle), `TrackObjects` Phase 12 placeholder, 3 measurement blocks (`RegionProps` with multi-select properties param, `PairwiseDistance` for tumour-microenvironment analyses, `Colocalization`), 3 registration blocks (`ComputeRegistration`/`ApplyTransform`/`RegisterSeries`), `AxisProjection`+`SelectSlice`, 4 scalar math blocks plus `ImageCalculator` (with AST-whitelisted expression mode for FRET ratios; variadic deferred to ADR-029 per Q-IMG-5), 5 visualization blocks producing `Artifact` outputs, and 4 interactive AppBlocks (`FijiBlock` with the user's locked Windows Fiji path, `NapariBlock`, `CellProfilerBlock`, `QuPathBlock`). Twelve resolved open questions cover the TIFF metadata protocol location (Q-IMG-1), Cellpose CPU fallback (Q-IMG-2), metadata propagation under shape-changing transforms with `Resize` updating `pixel_size` (Q-IMG-3), `AxisSplit` filename derivation rule (Q-IMG-4), `ImageCalculator` 0.1.0 fixed-port subset (Q-IMG-5), `Mask` strict dtype enforcement via `_validate_extra` (Q-IMG-6), `Label` requires-at-least-one-slot rule (Q-IMG-7), `LoadImage` unsupported-extension behaviour with no fallback (Q-IMG-8), the `iterate_over_axes`-vs-direct-indexing rule (Q-IMG-9), the 128 MiB lazy threshold (Q-IMG-10), `SaveImage` length-1-vs-N path semantics (Q-IMG-11), and `RegionProps` Collection-input concatenation pattern (Q-IMG-12). Each ticket section follows the Phase 10 12-subsection contract: source ADR sections, files to create, files to modify, new tests (each ticket lists 5-15 specific test names), implementation details with code sketches, acceptance criteria checklist, out-of-scope notes, dependencies, diff size estimate, suggested workflow gate ticket title. Cross-reference table maps every ticket to ADR-027 / ADR-028 / Addendum 1 sections; dependency graph identifies the critical path (T-IMG-001 → T-IMG-002 → T-IMG-019); recommended chained PR order shows where parallel work is permitted. Universal rules section mirrors Phase 10 standards §4 with two added imaging-specific rules: optional dependencies declared in extras with `requires_<extra>` pytest markers, and metadata propagation per ADR-027 D5 mandatory. Universal acceptance criteria add a worker subprocess round-trip smoke test for every block. E2E test sketch in §11 covers the imaging-only sub-workflow against the four `K562_*_2845`/`K562_*_spectra` test images at `C:\Users\jiazh\Desktop\workspace\Example\images\` (with parens and spaces in filenames preserved verbatim per master plan §8) and acknowledges the cross-plugin extension where `ExtractSpectrum` (specified in `docs/specs/phase11-srs-block-spec.md`) consumes the masks plus the spectra branch. Block list, type definitions, package layout, entry-point registration, and all locked decisions per Phase 11 master plan §2.4 are documented verbatim — this spec makes ZERO architectural decisions, only records them. Enables the Phase 11 implementation cascade for the `scieasy-blocks-imaging` 0.1.0 release. (@claude, 2026-04-07, branch: docs/issue-298/phase11-imaging-spec, session: 20260407-011502-phase-11-scieasy-blocks-imaging-specific)
- [#299] Phase 11 SRS plugin block specification — `docs/specs/phase11-srs-block-spec.md` (~1900 lines). Documentation-only spec defining the implementation contract for `scieasy-blocks-srs` per master plan §2.4 SRS PLUGIN. Locks one type (`SRSImage` subclassing imaging's `Image` with `required_axes = frozenset({"y","x","lambda"})` and a `Meta` model carrying `wavenumbers_cm1`, `laser_power`, `integration_time`, four digitizer parameters, and pump/Stokes wavelengths) and 11 blocks across three categories: 4 preprocessing (`SRSCalibrate` digitizer inversion + `Image→SRSImage` type conversion, `SRSBaseline` polynomial/rubber_band/rolling_ball_spectral, `SRSDenoise` PCA/SVD/wavelet/BM4D, `SRSNormalize` SNV/MSC/vector/area/peak_area), 5 component analysis (`SRSVCA` Vertex Component Analysis, `SRSUnmix` NNLS with auto-VCA fallback, `SRSPCA`, `SRSICA` FastICA, `SRSKMeansCluster`), 2 spectral extraction (`ExtractSpectrum` long-format DataFrame from Mask/Label ROIs, `BandRatio` two-band intensity ratio → 2D `Image`). No `RamanSpectrum` / `Spectrum` class — spectra are returned as plain `DataFrame`. No separate `LoadSRS` block — SRS ingress reuses imaging plugin's `LoadImage` then chains `SRSCalibrate`. Explicitly removed per user feedback: `SRSDespike` (no cosmic rays in SRS), `SRSWavenumberCalibrate` (systems pre-calibrated), `SRSMCR_ALS`, `SRSNMF`, `PeakFind`, `PeakFit`, all SRS-specific measurement blocks (`LipidDropletQuantify`, `ProteinLipidRatio`, `WaterContentMap`, `CellDryMassMap`), and any ALS-flavoured baseline correction. The spec walks the full Phase 10-style 12-subsection per-ticket pattern across 15 T-SRS tickets (T-SRS-000 package skeleton, T-SRS-001 type, T-SRS-002 through T-SRS-012 blocks, T-SRS-013 entry-point wiring, T-SRS-014 cross-plugin E2E test). Six open questions resolved: re-calibration detection via type+meta guards, lambda/c coexistence via `np.moveaxis` broadcast, `ExtractSpectrum` long format with `(region_id, wavenumber_cm1, intensity)` columns, `SRSUnmix` auto-VCA fallback when references absent, `SRSKMeansCluster` spectral feature shape, `SRSBaseline` default `polynomial` order=3. Cross-plugin dependency on `scieasy-blocks-imaging` declared one-way (srs imports imaging types; reverse forbidden). E2E integration test (T-SRS-014) consumes the four real test images at `Example/images/{K562_L_2845,K562_UL_2845,K562_L_spectra,K562_UL_spectra} (uV).tif` and exercises the full pipeline `LoadImage → Denoise → CellposeSegment → SRSCalibrate → ExtractSpectrum → SaveData`, gated behind `pytest.mark.requires_imaging`. (@claude, 2026-04-07, branch: docs/issue-299/phase11-srs-spec, session: 20260407-011711-phase-11-srs-plugin-spec-docs-specs-phas)
- [#297] ADR-029 preliminary: variadic port count and per-instance port editor (scope pending discussion). Reserves the ADR-029 namespace and documents the problem space for the count dimension of dynamic-port behaviour that ADR-028 Addendum 1 explicitly deferred. **Contains no architectural decisions** — every section that would normally hold a "Decision" reads "Pending discussion". The body documents the current state of `AIBlock` (30-line stub whose `run()` raises `NotImplementedError`, no port lists declared) and `CodeBlock` (163 lines, single static `data` input port and `result` output port both typed `DataObject`), explains why variadic ports are needed (concrete use cases: AI block taking N images and producing M tables, R-script ensemble averaging with variable fan-in, Python preprocessing fork with multiple typed outputs, future ensemble blocks), distinguishes the count dimension from Addendum 1's type dimension (fixed-count enum-driven types vs. variable-count user-declared types per port), and explains why the design discussion is being deferred until after the Phase 11 plugin cascade lands. Lists ten open questions Q1–Q10 each with 2–4 candidate answer directions A/B/C/D none of which are endorsed: Q1 where the variadic port list is stored (inline in `self.config`, new `self.port_config`, sidecar JSON, per-instance `BlockSpec`), Q2 how the GUI renders add/remove port controls (generic `variadic_port_list` widget, per-block React component, side panel, text-declared in script body), Q3 how validation handles variadic ports (treat user-declared like class-declared, fall back to `Any`, defer to runtime, hybrid structural-plus-runtime), Q4 how the scheduler routes through variadic ports (no change, per-dispatch effective-port read, special variadic dispatch path), Q5 how `_reconstruct_one` (worker subprocess) handles variadic ports given ADR-027 Addendum 1's typed reconstruction contract (per-port `type_chain` in payload, sidecar pre-resolved mapping, restrict to scan-time-resolvable types), Q6 whether AIBlock needs a `port_template` for the palette preview (zero ports, declared template, sentinel "+" badge, tooltip-only), Q7 whether CodeBlock auto-infers ports from script AST or user declares explicitly (manual editor only, Python AST auto-infer only, hybrid by language, side-load `block.yaml`), Q8 how `BlockSpec` represents a variadic block at scan time (single bool flag, separate input/output bools, capability enum, absence of port lists), Q9 whether variadic mode mixes with Addendum 1's enum `dynamic_ports` mapping (compose freely, mutually exclusive, compose with restrictions, defer), Q10 wire format for variadic ports in engine→worker payload (sidecar field, self-describing per-port metadata, CompositeData envelope). Scope statement enumerates what would be in scope (extensions to `get_effective_*_ports()`, frontend port editor widget, backend per-instance port storage, `BlockSpec` capability flag, validator updates, worker round-trip updates) once promoted plus what is explicitly out of scope (per-port auth, lazy port instantiation, runtime type inference, migration tooling, AI port suggestion). Adds `TODO(ADR-029)` paragraphs to `AIBlock` and `CodeBlock` class docstrings as forward pointers. Decision / Alternatives considered / Consequences sections all read "Pending discussion". Status `draft — scope pending discussion`. Hard freeze: any implementation PR that touches variadic AIBlock, variadic CodeBlock, or "add port" / "remove port" GUI controls must reference ADR-029 explicitly until it is promoted to `proposed`. ~739 lines appended to `docs/adr/ADR.md`. (@claude, 2026-04-07, branch: docs/issue-297/adr-029-preliminary, session: 20260407-011117-adr-029-preliminary-variadic-port-count)
- [#295] ADR-028 Addendum 1: Dynamic port override mechanism and GUI consequences. Documentation-only Addendum that introduces a minimal dynamic-port mechanism on the `Block` ABC (two override methods `get_effective_input_ports()` / `get_effective_output_ports()` + an enum-only declarative `dynamic_ports: ClassVar[dict | None]` mapping), locks the `BlockNode.tsx` GUI hardcoding removal (three `blockType === "io_block"` checks generalised to `category === "io"` plus Browse button generalisation), and **supersedes ADR-028 §D3** ("Seven core loader/saver pairs"): the six concrete loader/saver classes become two dynamic blocks `LoadData` and `SaveData` with twelve private module-level dispatch functions (`_load_array`, `_load_dataframe`, `_load_series`, `_load_text`, `_load_artifact`, `_load_composite_data`, and the symmetric `_save_*` set) in `load_data.py` / `save_data.py`. The user explicitly chose private functions over helper classes (Q-B) and enum-only mapping over a mini-DSL (Q-A). AI variadic port count and CodeBlock variadic port count are explicitly out of scope and deferred to ADR-029 (preliminary). Nine discussion-point decisions D1'–D9' (with prime marks to distinguish from ADR-028's D1–D14), four rejected alternatives (no dynamic mechanism, hidden classes with meta-palette, Pydantic discriminated union, combined variadic in same addendum). Detailed impact scope enumerates 7 backend files to modify, 4 backend files to create, 2 frontend files to modify, 5 test files to create, and an explicit "not affected" list covering Phase 10 core types / engine / other block bases / ADR-027 Addendum 1 worker reconstruction contract. Framework read-path migration: `Block.validate()`, `ProcessBlock.run()`, `workflow/validator.py`, `api/routes/blocks.py` all switch from `type(block).input_ports` to `block.get_effective_*_ports()`. Plugin IO blocks (`LoadImage`, `LoadMSRawFile`, etc.) remain static and get port colours via the existing `resolveTypeColor()` type-hierarchy walk — no per-plugin frontend work. 591 lines appended to docs/adr/ADR.md. (@claude, 2026-04-07, branch: docs/issue-295/adr-028-addendum-1-dynamic-ports, session: 20260407-005014-adr-028-addendum-1-dynamic-ports-overrid)
- [#293] ADR-028: IOBlock architectural refactor — plugin-owned IO pattern. Documentation-only ADR that records the Phase 11 decision to delete the central adapter registry and refactor `IOBlock` into an abstract base class whose concrete subclasses are per-type loaders/savers. The eight bundled adapters are redistributed: `csv`/`parquet`/`zarr`/`generic` merge into core `LoadDataFrame`/`LoadArray`/`LoadArtifact` implementations; `tiff`/`mzxml`/`h5ad`/`fcs` move verbatim to their target plugin packages (`scieasy-blocks-imaging`, `scieasy-blocks-lcms`, future `scieasy-blocks-singlecell`, future `scieasy-blocks-flow`). Core ships seven concrete loader/saver pairs (Array/DataFrame/Series/Text/Artifact/CompositeData; bare DataObject has no loader). The `scieasy.adapters` entry-point group is deleted (ADR-025 §6 superseded); plugins register IO blocks via the existing `scieasy.blocks` entry-point group. Pickle support is first-class but opt-in via `allow_pickle: bool = False` on LoadDataFrame/LoadSeries/LoadArray with an explicit security warning. Additional first-class format paths added: `.tsv`, `.pkl`, `.pickle`, `.npy`, `.npz`, `.parquet` for single-column Series. 14 discussion-point decisions D1–D14 plus four rejected alternatives (retrofit adapter with type_chain, subclass_of ClassVar, gradual deprecation, unified TypeRegistry-reader graph). Detailed impact scope enumerates 13 files to delete, 3 files to rewrite, 14 files to create, 9 documentation files to update in follow-up PRs, and explicit "not affected" list covering core types / engine / other block bases. Migration guide for workflow YAML files (`IOBlock(format=...)` → specific loader blocks). Enables Phase 11 Track 2 (scieasy-blocks-imaging), Track 3 (scieasy-blocks-srs), Track 4 (scieasy-blocks-lcms). 752 lines appended to docs/adr/ADR.md. (@claude, 2026-04-06, branch: docs/issue-293/adr-028-ioblock-refactor, session: 20260406-233129-adr-028-ioblock-architectural-refactor-a)
- [#251] Stage 10.1 Part 1: Palette research, skeleton, and design doc — adds category ClassVar on Block, source/package_name fields on BlockSummary (with safe defaults), optional source?/package_name? on TS type, TODO markers in _infer_category/_summary/BlockPalette.tsx, and 13 skipped test stubs for Agent B to implement in Part 2; includes docs/design/stage-10-1-palette.md (661 lines) with current state analysis, proposed changes, test plan, and migration notes (@claude, 2026-04-06, branch: feat/issue-251/stage-10-1-skeleton, session: 20260406-015803-stage-10-1-part-1-research-skeleton-and)
- [#290] T-014: worker subprocess typed reconstruction per ADR-027 D11 + Addendum 1 §1. Replaces the T-013 stub in `scieasy.core.types.serialization` with full `_reconstruct_one` and `_serialise_one` implementations: each delegates to the resolved class's `_reconstruct_extra_kwargs` / `_serialise_extra_metadata` classmethod hooks (T-013) for base-class-specific fields, populates the framework/meta/user three-slot model (T-005), and uses `TypeRegistry.resolve(type_chain)` (T-012) to look up the most specific registered class. `worker.reconstruct_inputs` and `worker.serialise_outputs` are refactored to delegate per-item to the new helpers, returning TYPED `DataObject` instances (not `ViewProxy`) so block authors can use `item.meta`, `item.with_meta()`, and `isinstance` checks naturally inside their subprocesses. Lazy loading is preserved at the method level: reconstructed instances have `storage_ref` set but do not read payload data until `to_memory()` / `view()` / `sel()` / `iter_over()` is called. `worker.main()` warms a module-level lazy `TypeRegistry` singleton (`_get_type_registry` in `serialization.py`) on startup that scans core built-ins via `scan_builtins()` and attempts plugin entry-points via `_scan_entrypoint_types()` inside a `contextlib.suppress(Exception)` so a broken plugin never takes down core reconstruction. The `ViewProxy` import is removed from `worker.py`; `ViewProxy` remains as the return type of `Array.view()` for blocks that need explicit chunk-by-chunk reading. Construction failures inside `_reconstruct_one` are wrapped with the offending class name (`"Failed to reconstruct {cls.__name__} from payload: {original_error}"`) so operators can locate broken payloads quickly. 38 new tests in `tests/engine/test_worker_typed_reconstruction.py` covering `_reconstruct_one` class resolution (type_chain fallback, Meta validation, framework round-trip, user dict preservation), `_serialise_one` metadata sidecar shape, round-trip equivalence for all six base classes (Array / Series / DataFrame / Text / Artifact / CompositeData), `reconstruct_inputs` dispatcher cases (single typed item, Collection, scalar pass-through), `serialise_outputs` round-trip, reconstructed-instance behaviour (with_meta, isinstance, lazy payload), the TypeRegistry singleton helper, and construction-failure error wrapping. `tests/core/test_base_class_reconstruction_hooks.py` deletes the four T-013 stub-raises tests (`test_composite_reconstruct_raises_until_t014`, `test_composite_serialise_raises_until_t014`, `test_serialization_stub_reconstruct_raises`, `test_serialization_stub_serialise_raises`) and replaces them with positive composite round-trip tests plus smoke tests for the real `_reconstruct_one` / `_serialise_one` bodies on bare `DataObject`. `tests/engine/test_worker.py` and `tests/engine/test_local_runner.py` are updated where they asserted `ViewProxy` return types to assert the typed class instead (`Array` for the imaging-shaped payload, `DataObject` for the bare fallback). Capstone of Phase 10 — every previous ticket (T-001..T-013) was preparing for this moment. Stacked on PR #289. (@claude, 2026-04-06, branch: feat/issue-290/T-014-worker-typed-reconstruction, session: 20260406-201435-t-014-worker-subprocess-typed-reconstruc)
- [#288] T-013: six base-class reconstruction hook pairs per ADR-027 Addendum 1 §2. Each of DataObject, Array, Series, DataFrame, Text, Artifact, CompositeData gains a `_reconstruct_extra_kwargs(metadata: dict) -> dict` classmethod (returns the constructor kwargs to extract from the wire-format metadata sidecar) and a `_serialise_extra_metadata(obj) -> dict` classmethod (the symmetric counterpart that writes into the sidecar). DataObject returns empty dicts by default; Array extracts `axes`/`shape`/`dtype`/`chunk_shape` (tuples↔lists, dtype stringified); Series extracts `index_name`/`value_name`/`length`; DataFrame extracts `columns`/`row_count`/`schema`; Text extracts `content`/`format`/`encoding`; Artifact extracts `file_path` (Path↔str)/`mime_type`/`description`; CompositeData recursively delegates to `_reconstruct_one` / `_serialise_one` from a new `scieasy.core.types.serialization` module via lazy import inside the classmethod body to break the load-time cycle. The `serialization` module is a T-013 stub whose `_reconstruct_one` / `_serialise_one` raise NotImplementedError with a pointer to T-014; T-014 lands the full worker reconstruction logic, replacing the stub bodies while keeping the signatures locked. Composite empty-slots paths short-circuit before touching the stub, so T-013 ships without breaking plugin tests that construct an empty composite. Subclass overrides type their `obj` parameter as `DataObject` (not the narrower subclass) to respect Liskov with the base classmethod and assert-narrow inside the body. 39 new tests in `tests/core/test_base_class_reconstruction_hooks.py` covering per-class kwargs extraction, per-class metadata emission, round-trip equivalence on the five flat base classes, composite stub-raises delegation, serialization module importability, parametrized hook discovery on all seven classes, and the plugin-subclass `super()` override chaining pattern. Stacked on PR #287. (@claude, 2026-04-06, branch: feat/issue-288/T-013-base-class-reconstruction-hooks, session: 20260406-195928-t-013-base-class-reconstruction-hooks-pe)
- [#286] T-012: TypeRegistry.resolve(type_chain) and Meta-class validation per ADR-027 D11 + Addendum 1 §3. resolve() is overloaded on argument type: passing a list[str] walks the chain rightmost-to-leftmost and returns the most specific registered class (or None) — used by worker subprocess reconstruction (T-014) to look up typed DataObject classes from serialised metadata; the legacy resolve(name: str) -> TypeSpec path is preserved for backward compatibility with existing callers (load_class, tests/core/test_types.py, tests/architecture/test_registries.py). _validate_meta_class() runs at registration time inside scan_builtins and _scan_entrypoint_types and rejects any subclass whose Meta is not a pydantic.BaseModel subclass, declares PrivateAttr fields, or fails JSON round-trip through model_dump(mode='json') / model_validate, with clear error messages pointing at the offending class. Meta classes whose required fields have no defaults skip the round-trip smoke test per the ADR (plugin authors are expected to write their own regression test). Broken plugin Meta is logged-and-skipped inside _scan_entrypoint_types so one bad plugin does not take down the whole registry; broken core-builtin Meta raises loudly inside scan_builtins. A new register_class(cls) convenience helper validates + builds a TypeSpec + registers in one call. All six core base classes (Meta=None per T-005) pass validation via a canary test that guards against future refactors. 19 new tests in tests/core/test_type_registry_resolve_and_meta.py. Stacked on PR #285. (@claude, 2026-04-06, branch: feat/issue-286/T-012-type-registry-resolve, session: 20260406-194916-t-012-typeregistry-resolve-meta-validati)
- [#284] T-011: scieasy.utils.axis_iter.iterate_over_axes(source, operates_on, func) utility per ADR-027 D3. Iterates user-provided func over all axes NOT in operates_on, calls func(slice_data, coord_dict) for each combination, stacks results back into a new Array of source's concrete class. Memory: O(one input slice + one output slice). Serial by design (no threads, no subprocesses) per ADR-027 D8. Metadata propagation: framework derived (with `derived_from=source.framework.object_id`), meta shared by reference, user shallow copy, axes preserved. Raises `BroadcastError` when operates_on is not a subset of source.axes, when source.shape is None, when slice outputs have inconsistent shapes, or when func returns arrays with a different number of dimensions than len(operates_on); user-raised errors propagate unchanged. Imports `BroadcastError` from the sibling `scieasy.utils.broadcast` module (ADR-027 D3 referenced `scieasy.core.exceptions` which does not yet exist in the repo) so one `except BroadcastError:` clause covers both utilities. Sibling of `broadcast_apply` for the "single Array, iterate extra dims" pattern. Replaces the T-011 `NotImplementedError` skeleton from PR #262; removes the corresponding smoke tests from `tests/core/test_phase10_skeleton.py`; adds 16 per-feature tests in `tests/utils/test_axis_iter.py` covering the ADR-027 D3 acceptance criteria plus edge cases (empty extra axes, func reducing dim count, subclass preservation, metadata propagation, user-func error propagation). Stacked on PR #283. (@claude, 2026-04-06, branch: feat/issue-284/T-011-axis-iter, session: 20260406-193821-t-011-scieasy-utils-axis-iter-iterate-ov)
- [#282] T-010: scieasy.utils.constraints port helper factories per ADR-027 D4 companion. Adds `has_axes(*required)` (superset check), `has_exact_axes(*axes)` (set equality), `has_shape(ndim)` (exact rank), `has_min_shape(ndim)` (minimum rank), `has_dtype(*dtypes)` (string-normalised dtype match), and `is_2d()` / `is_3d()` convenience aliases. Each factory returns a `Callable[[Collection], bool]` suitable for `InputPort(constraint=...)` — pure Python predicates that read instance attributes only, with no I/O, no `to_memory()` calls, and no storage-backend coupling (consistent with Phase 10 Level 1 laziness). Every returned callable iterates the collection once, short-circuits on first failure, treats missing attributes as a graceful `False` (not `AttributeError`), treats empty iterables as vacuously true, and carries a descriptive `__name__` / `__doc__` for `validate_port_constraint` breadcrumbs. Replaces the T-010 `NotImplementedError` skeleton from PR #262; removes the corresponding smoke tests from `tests/core/test_phase10_skeleton.py`; adds 31 per-feature tests in `tests/utils/test_constraints.py` including end-to-end integration through `InputPort` + `validate_port_constraint`. Stacked on PR #281. (@claude, 2026-04-06, branch: feat/issue-282/T-010-utils-constraints, session: 20260406-192835-t-010-scieasy-utils-constraints-per-adr)
- [#280] T-009: ProcessBlock setup/teardown lifecycle hooks per ADR-027 D7. New `setup(config)` method runs once per `run()` before iterating the input Collection and returns opaque state. New `teardown(state)` method runs in a finally block even on exception so blocks can release resources (close DB handles, free GPU memory). `process_item` signature gains an optional third parameter `state` (default `None`) that is threaded from `setup` through every per-item call to `teardown`. The default `run()` calls setup once, iterates with `state`, and calls teardown via try/finally. Backward-compat: pre-T-009 `process_item(self, item, config)` overrides continue to work because `run()` uses `inspect.signature` (`_process_item_takes_state`) to detect the override's arity and calls with 2 or 3 args accordingly. Built-in `TransformBlock` (the only built-in that overrides `process_item`) is updated to the new 3-arg form; other built-ins override `run()` directly and are unaffected. Cellpose-style heavy blocks can now load their model once per run instead of per item. (@claude, 2026-04-06, branch: feat/issue-280/T-009-process-block-lifecycle, session: 20260406-191910-t-009-processblock-setup-teardown-hooks)
- [#269] T-004: FrameworkMeta + scieasy.core.meta module per ADR-027 D5. FrameworkMeta is a frozen Pydantic v2 BaseModel populated at DataObject construction with `created_at` (UTC), `object_id` (UUIDv4 hex), `source`, `lineage_id`, and `derived_from`; its `derive(**changes)` method implements the ADR-027 D5 propagation rule. ChannelInfo is a frozen Pydantic BaseModel for plugin Meta classes describing acquisition channels (`name`, `dye`, `excitation_nm`, `emission_nm`); lives in core so plugins compose it without forcing plugin->plugin imports. `with_meta_changes(meta, **changes)` is the free-function helper backing `DataObject.with_meta()`, which lands in T-005. The Phase 10 skeleton's NotImplementedError placeholders in `src/scieasy/core/meta/` are replaced with the real implementation; the corresponding T-004 smoke tests in `tests/core/test_phase10_skeleton.py` are removed and superseded by 22 unit tests in the new `tests/core/test_framework_meta.py`. Stacked on PR #262 (@claude, 2026-04-06, branch: feat/issue-269/T-004-framework-meta, session: 20260406-174408-t-004-frameworkmeta-scieasy-core-meta-mo)
- [#267] T-003: PhysicalQuantity dataclass with Pydantic v2 integration per ADR-027 D6 + Addendum 1 §4. Frozen dataclass holding (value, unit) with kind-aware `to()` conversion, ordering, and equality. Pydantic BaseModel fields can declare `pixel_size: PhysicalQuantity` directly; round-trips through `model_dump_json` / `model_validate_json` as `{"value": ..., "unit": ...}`. Covers length, time, frequency, wavenumber kinds. Replaces the PR #262 skeleton; the PhysicalQuantity skeleton smoke tests are removed from `tests/core/test_phase10_skeleton.py` (other skeleton blocks for T-004/T-010/T-011 are untouched). (@claude, 2026-04-06, branch: feat/issue-267/T-003-physical-quantity, session: 20260406-173147-t-003-physicalquantity-per-adr-027-d6-ad)
- [#261] Phase 10 implementation skeleton and standards (T-000) — five new skeleton files (`src/scieasy/core/units.py`, `src/scieasy/core/meta/__init__.py`, `src/scieasy/core/meta/framework.py`, `src/scieasy/utils/constraints.py`, `src/scieasy/utils/axis_iter.py`) declaring the public class/function signatures for ADR-027 D3/D4/D5/D6 and ADR-027 Addendum 1 §4, with placeholder bodies that raise `NotImplementedError` pointing at the implementation ticket; new `docs/specs/phase10-implementation-standards.md` codifying the contract for the 14 follow-up implementation tickets (T-001..T-014) with files to create/modify, new tests, acceptance criteria, dependencies, estimated diff size, out-of-scope notes, and resolved open questions for `_reconstruct_one`/`_serialise_one` placement (`scieasy.core.types.serialization`) and `tests/integration/test_multimodal_workflow.py` migration strategy (`@pytest.mark.requires_imaging`); no existing source files under `src/scieasy/` are modified (@claude, 2026-04-06, branch: docs/issue-261/phase10-skeleton, session: 20260406-134939-phase-10-implementation-skeleton-and-sta)
- [#259] ADR-027 Addendum 1: clarify that worker subprocess returns typed DataObject instances (not ViewProxy). Resolves the contradiction between ADR-027 D11's pseudocode (returns ViewProxy) and ADR-027 D5/D7's user-facing API (item.meta, with_meta, isinstance checks). Specifies the _reconstruct_one / _serialise_one helpers, the _reconstruct_extra_kwargs / _serialise_extra_metadata classmethod hook contract on each base class, the Meta Pydantic constraints (frozen, no PrivateAttr, JSON-round-trippable), the PhysicalQuantity Pydantic v2 __get_pydantic_core_schema__ integration, and the demoted role of ViewProxy after this Addendum (@claude, 2026-04-06, branch: docs/issue-259/adr-027-addendum-1, session: 20260406-132622-adr-027-addendum-1-clarify-worker-subpro)

### Removed

- [#311] Delete `src/scieasy/blocks/process/builtins/register.py` 1-line stub (T-TRK-002). Phase 11 master plan §2.5 sub-1a + standards doc §9.1 T-TRK-002 — pure removal of a Phase 11 placeholder docstring that declared `RegisterBlock — image registration (cross-modal alignment).` with no class body. Image registration belongs in `scieasy-blocks-imaging` (T-IMG-027/028/029 per the imaging spec); the core `process/builtins/` directory is reserved for plain DataObject collection ops, so the dead stub is removed rather than implemented. Verified `__init__.py` did not import the stub and no test or source file referenced `scieasy.blocks.process.builtins.register` or `RegisterBlock` before deletion. Sprint A Level A0 parallel ticket. (@claude, 2026-04-07, branch: refactor/issue-311/T-TRK-002-delete-register-stub, session: 20260407-165952-t-trk-002-delete-process-builtins-regist)

### Changed

- [#315] T-TRK-003: relocate the smoke-test `TransformBlock` from `src/scieasy/blocks/process/builtins/transform.py` to `tests/fixtures/noop_block.py` as `NoopBlock` (`type_name = "noop"`), per master plan §2.5 sub-1a and `docs/specs/phase11-implementation-standards.md` T-TRK-003. The block was never a placeholder — it is the smoke-test fixture used by the API/frontend and execution-engine tests, just miscategorised as a production builtin. Body preserved verbatim (identity pass-through with optional sleep — load-bearing for the smoke tests). Adds `tests/fixtures/__init__.py`, `tests/fixtures/noop_block.py`, `tests/fixtures/test_images.py` (Q6 E2E test image path constants — four `.tif` `Path` constants plus `SEGMENTATION_IMAGES`/`SPECTRA_IMAGES` derived lists), and `tests/fixtures/test_noop_block.py` (instantiation + `process_item` identity + `run` Collection pass-through smoke tests). Deletes `src/scieasy/blocks/process/builtins/transform.py`. Updates `src/scieasy/blocks/registry.py` to remove the `TransformBlock` import and the corresponding entry in `_scan_builtins` so the production registry no longer ships `process_block` as a builtin. Doc-external addition (`tests/conftest.py`): a session-wide patch on `BlockRegistry._scan_builtins` re-registers `NoopBlock` and aliases the legacy `"process_block"` type_name to it for the test session only — required because ~12 existing test files reference `block_type="process_block"` via the registry, and rewriting them is out of scope for T-TRK-003 per the standards doc estimated diff and master plan §6.7 "no silent scope expansion". Production code never sees `NoopBlock` and never exposes the `"process_block"` alias. Single test update in `tests/api/test_blocks.py` switches the palette assertion from `type_name == "process_block"` to the new canonical `type_name == "noop"`; schema/connection lookups still use the legacy alias and remain unchanged. `git grep "TransformBlock"` now returns only historical doc/CHANGELOG entries and explanatory comments. `pytest -x --no-cov` green (1881 passed, 14 skipped); `ruff check`/`ruff format --check`/`mypy src/scieasy --ignore-missing-imports`/`lint-imports` all clean. (@claude, 2026-04-07, branch: refactor/issue-315/T-TRK-003-move-transform-to-noop-fixture, session: 20260407-170501-t-trk-003-move-transform-py-to-tests-fix)
- [#314] T-TRK-011: replace deprecated `ManualReviewBlock` references with the `AppBlock` pattern in `CLAUDE.md`, `docs/architecture/ARCHITECTURE.md`, and `docs/guides/block-sdk.md`. CLAUDE.md §2.5 now points to `AppBlock` (`docs/guides/block-sdk.md` §3.4.1) as the canonical implementation of manual review steps. ARCHITECTURE.md block-palette tree drops the `manual_review` Core entry in favour of `app_block` and adds a one-paragraph pointer explaining that `AppBlock` already implements the `IDLE → READY → RUNNING → PAUSED → RUNNING → DONE` lifecycle that human-in-the-loop steps require. block-sdk.md gains a new §3.4.1 "Writing a manual review step using AppBlock to open Fiji" with a complete, syntactically valid `ManualFijiReview` example, lifecycle walk-through, tuning notes (`watch_timeout`, `output_patterns`, `done_marker`, cancellation), and forward-reference to T-IMG-034 (`FijiBlock`) for the imaging-plugin implementation. Per Phase 11 master plan §2.5 sub-1c and `docs/specs/phase11-implementation-standards.md` T-TRK-011. Sprint A Level A0 parallel (docs only). Historical references in `docs/adr/ADR.md` are intentionally untouched (ADRs are immutable). (@claude, 2026-04-07, branch: docs/issue-314/T-TRK-011-manualreview-clarification, session: 20260407-170307-t-trk-011-claude-md-and-architecture-md)
- [#277] T-008: migrate all non-core tests off the deleted core `Image`/`FluorImage`/`SRSImage`/`MSImage`/`Spectrum`/`PeakTable`/`AnnData`/`SpatialData` classes per ADR-027 D2. Replaces the `Array as Image` (and analogous `CompositeData as AnnData`, `DataFrame as PeakTable`, `Series as Spectrum`) `TODO(T-008)` shims that T-006 and T-007 left in 8 test files with proper `Array(axes=[...])` constructions or local `Image(Array)` test fixture subclasses (mirroring the pattern in `tests/core/test_types.py`). Also migrates 4 additional test files (`tests/blocks/test_adapters.py`, `tests/blocks/test_app_block.py`, `tests/engine/test_checkpoint.py`, `tests/integration/test_block_sdk_e2e.py`) that still had raw `from scieasy.core.types.array import Image` imports broken since T-006. `tests/integration/test_multimodal_workflow.py` is gated behind `pytest.importorskip("scieasy_blocks_imaging")` plus `pytestmark = pytest.mark.requires_imaging` per phase 10 spec Q2 (skip-via-marker), and the `requires_imaging` pytest marker is registered under `[tool.pytest.ini_options]` in `pyproject.toml`. `tests/architecture/test_type_system.py` is rewritten to test the schema-declaration capability of `Array` and `CompositeData` via local fixture subclasses (`_ImageFixture` declares `required_axes`, `_AnnDataFixture` declares `expected_slots`); the old per-subclass invariant tests pinning the deleted core domain classes are removed. Side cleanup: removed three stale `[project.entry-points."scieasy.types"]` entries (`image`, `spectrum`, `peak_table`) from `pyproject.toml` that pointed at modules where the classes no longer exist; the architecture-test `test_entry_point_resolves_to_class` was failing on those references. Follow-up issue #278 tracks the remaining `obj.metadata` deprecation reads in `src/scieasy/blocks/process/builtins/filter_collection.py:58` and `src/scieasy/blocks/app/bridge.py:52` which are intentionally out of scope for this test-only ticket. (@claude, 2026-04-06, branch: feat/issue-277/T-008-test-migration, session: 20260406-185653-t-008-test-migration-image-to-array-per)
- [#275] T-007: Audit and three-slot integration of remaining 5 core base classes (Series, DataFrame, CompositeData, Text, Artifact) per ADR-027 D2 + D5. Each base class now has a `with_meta()` override that propagates its specific constructor arguments alongside the standard framework/meta/user/storage_ref slots, matching the pattern T-006 introduced for Array. REMOVES `Spectrum`/`RamanSpectrum`/`MassSpectrum` from `core/types/series.py`, `PeakTable`/`MetabPeakTable` from `core/types/dataframe.py`, and `AnnData`/`SpatialData` from `core/types/composite.py` per ADR-027 D2 — these will be added to their respective plugin packages (`scieasy-blocks-spectral`, `scieasy-blocks-singlecell`, `scieasy-blocks-spatial-omics`) in a future Phase 10 work item. `scieasy.core.types.__all__` now exports exactly the seven core base types plus `Collection`/`TypeRegistry`, and `TypeRegistry.scan_builtins()` registers only those seven classes. `api/runtime.py` chart-preview dispatch no longer hard-imports `Spectrum` — it now matches `Series` by name plus any type containing `"Spectrum"` so plugin-provided spectra still render as charts. Core tests that referenced the deleted subtypes use local T-007 shim subclasses (same pattern T-006 established for `Image`); `tests/architecture/` and `tests/blocks/` carry alias imports with TODO(T-008) markers. New `tests/core/test_other_base_classes_audit.py` (24 tests) regression-guards the constructor + with_meta pattern on each base class. (@claude, 2026-04-06, branch: feat/issue-275/T-007-other-base-classes-audit, session: 20260406-184210-t-007-other-base-classes-audit-per-adr-0)
- [#273] T-006: Array now uses instance-level axes per ADR-027 D1 — `axes` is a required keyword-only constructor argument carrying the axis labels (e.g., `["t", "z", "c", "lambda", "y", "x"]` for 6D fluorescence/spectral imaging). Class-level schema fields `required_axes` (frozenset), `allowed_axes` (frozenset | None), and `canonical_order` (tuple) let subclasses constrain the accepted axis sets without subclassing per dimensional combination. Adds `Array.sel(**kwargs)` and `Array.iter_over(axis)` per ADR-027 D4 with Level 1 laziness (one slice per iteration step, metadata preserved per ADR-027 D5). Overrides `DataObject.with_meta()` to propagate Array-specific constructor kwargs (axes/shape/dtype/chunk_shape). `TypeSignature.from_type()` now captures `required_axes` for Array subclasses with a non-empty schema so port-level compatibility checks can enforce axis requirements (ADR-027 D1). REMOVES `Image`, `MSImage`, `SRSImage`, `FluorImage` from core per ADR-027 D2 — these now live in the `scieasy-blocks-imaging` plugin. Internal call sites in `tiff_adapter.py` and `api/runtime.py` migrated to use `Array(axes=...)` directly; `TypeRegistry.scan_builtins` no longer registers the removed subtypes. Non-core test files that imported `Image` carry a T-008 shim (`from scieasy.core.types.array import Array as Image`) so collection stays green until T-008 lands the proper migration; `tests/core/` is fully migrated in this PR. (@claude, 2026-04-06, branch: feat/issue-273/T-006-array-6d-axes, session: 20260406-181626-t-006-array-6d-instance-level-axes-per-a)
- [#271] T-005: DataObject metadata is now stratified into three slots per ADR-027 D5: `framework` (FrameworkMeta, immutable framework-managed), `meta` (typed Pydantic BaseModel, None on the base class, overridden by plugin subtypes via the new `Meta` ClassVar), and `user` (free-form JSON-serialisable dict). Adds `with_meta(**changes)` instance method that returns a new instance with updated meta and a freshly-derived FrameworkMeta (`derived_from = self.framework.object_id`). Backward-compat: `DataObject.metadata` property and `DataObject(metadata=...)` constructor kwarg both still work and emit DeprecationWarning; both removed in Phase 11. Subclasses with extra `__init__` parameters (Array, CompositeData) must override `with_meta` themselves — T-006/T-007 own those overrides. (@claude, 2026-04-06, branch: feat/issue-271/T-005-dataobject-three-slot, session: 20260406-175948-t-005-dataobject-three-slot-metadata-per)
- [#265] T-002: ResourceManager.gpu_slots default changes from 0 to None, triggering best-effort auto-detect via torch.cuda.device_count() then nvidia-smi -L per ADR-027 D10. Existing GPU blocks become dispatchable on CUDA-enabled machines without explicit project-config override. ResourceRequest.max_internal_workers and effective_cpu property formally activated for ADR-027 D8 thread-policy context. (@claude, 2026-04-06, branch: feat/issue-265/T-002-resource-manager-gpu-autodetect, session: 20260406-171241-t-002-resourcemanager-gpu-auto-detect-pe)
- [#257] Update ARCHITECTURE.md, PROJECT_TREE.md, and docs/guides/block-sdk.md to reflect ADR-018 Addendum 1 and ADR-027 — documentation only. §4.1 rewritten for 7-type core + plugin-provided domain subtypes and 6D instance-level axes; §4.5 documents iterate_over_axes utility; §5.1 adds setup/teardown hooks; §5.4 states core/plugin boundary; §6.1 documents scheduler concurrency model with asyncio.create_task + _active_tasks; §6.4 documents ResourceManager GPU auto-detect and ResourceRequest.max_internal_workers; Appendix A gains plugin prerequisite note. block-sdk.md gains new sections on setup/teardown, domain metadata conventions, L2 fan-out parallelism pattern, and thread policy; all core-Image imports in examples replaced with plugin-package imports (@claude, 2026-04-06, branch: docs/issue-257/phase10-arch-updates, session: 20260406-045449-phase-10-architecture-and-dev-guide-doc)

### Added

- [#255] ADR-018 Addendum 1 (scheduler concurrency implementation) and ADR-027 (Phase 10 core type system and block runtime refinements) — documentation only, codifying Phase 10 architectural decisions on 6D axes, domain-type extraction from core, iterate_over_axes utility, lazy iter_over/sel, stratified Pydantic metadata, PhysicalQuantity units, setup/teardown hooks, thread policy, L2 fan-out parallelism, ResourceManager GPU auto-detect, and worker TypeRegistry scan (@claude, 2026-04-06, branch: docs/issue-255/phase10-adrs, session: 20260406-043746-phase-10-architectural-adrs-scheduler-co)

### Fixed

- [#247] Sandbox `dry_run_generated_code()` — restricted `__builtins__` to a safe whitelist (no `open`, `exec`, `eval`, `compile`, `globals`, etc.), restricted `__import__` to a module whitelist (typing, abc, dataclasses, enum, collections, functools, math), thread-based timeout (5s) for runaway code (@claude, 2026-04-06, branch: fix/issue-247/exec-sandbox)
- [#263] T-001: Scheduler concurrency fix per ADR-018 Addendum 1 — split `DAGScheduler._dispatch` into a synchronous prelude plus an async `_run_and_finalize` task body, add the `_active_tasks` registry, add `_dispatch_newly_ready` retry helper, add `_cancel_active_tasks_on_shutdown` cleanup, wrap `execute()`/`execute_from()` in `try/finally`, update `_check_completion` to also require `not _active_tasks`, branch `_on_cancel_block`/`_on_cancel_workflow` on ProcessHandle presence (terminate vs task.cancel()), and name dispatched tasks `dispatch:{block_id}`. Independent DAG branches now execute concurrently, restoring the behaviour ADR-018 §5 promised. Six new scheduler concurrency tests under `tests/engine/test_scheduler_concurrency.py`. (@claude, 2026-04-06, branch: feat/issue-263/T-001-scheduler-concurrency, session: 20260406-165314-t-001-scheduler-concurrency-fix-per-adr)
- [#232] Fix frontend AI chat code review issues — AIGenerateBlockResponse type drift (add validation_report, category), ErrorBanner timer reset via useCallback, Enter-key send test (@claude, 2026-04-05, branch: feat/issue-232/frontend-ai-chat)

### Added

- [#232] Frontend AI Chat integration — real API dispatch, code preview, loading states, error handling (@claude, 2026-04-05, branch: feat/issue-232/frontend-ai-chat, session: 20260405-235013-feat-232-frontend-ai-chat-integration-wi)
- [#237] Fix canvas annotation code review issues — MiniMap crash guard, DAG edge KeyError guard, random position offset (@claude, 2026-04-06, branch: fix/issue-237/code-review-fixes)

### Added

- [#233] Parameter optimization -- LLM-powered param suggestions with schema validation (@claude, 2026-04-05, branch: feat/issue-233/param-optimizer, session: 20260405-235030-feat-233-parameter-optimization-backend)
- [#230] Type Generator + Validator Stages 4-5 — generate_type() with LLM-driven code generation, family inference, retry loop; dry_run_generated_code() stage 4, validate_port_contracts() stage 5, validate_generated_type() pipeline; 45 new tests (@claude, 2026-04-05, branch: feat/issue-230/type-generator-validator, session: 20260405-232504-phase-9-1c-type-generator-validator-stag)
- [#229] Block Generator + API endpoint wiring — generate_block() pipeline with category inference, prompt construction, LLM call, code extraction, validation, retry loop; POST /api/ai/generate-block endpoint; 33 tests (@claude, 2026-04-05, branch: feat/issue-229/block-generator, session: 20260405-232453-phase-9-1b-block-generator-api-endpoint)
- [#241] Phase 10 Roadmap v0.3 — 4-stage integration plan: palette refinement, 5 domain plugin packages, block-level testing, demo workflows (@claude, 2026-04-05, branch: docs/phase10-roadmap-v0.3, session: 20260405-234433-phase-10-roadmap-v0-3-integration-domain)
- [#237] Canvas annotation and group frame nodes -- _annotation (floating text note) and _group (resizable dashed-border frame) with toolbar buttons, DAG skip for _-prefixed nodes, 13 tests (@claude, 2026-04-05, branch: feat/issue-237/canvas-annotations, session: 20260405-232810-canvas-annotations-and-group-frames-237)
- [#228] LLM Provider Foundation — LLMProvider protocol, AnthropicProvider, OpenAIProvider, AIConfig, get_provider() factory, extract_code/extract_json parsers, [ai] optional deps, 64 tests (@claude, 2026-04-05, branch: feat/issue-228/llm-provider-foundation, session: 20260405-225710-phase-9-1a-llm-provider-foundation-228)
- [#218] Block SDK end-to-end integration tests — 30 tests covering registry entry-point roundtrip, Tier 1 drop-in scanning, adapter priority enforcement, TypeRegistry entry-points, and cross-cutting integration (@claude, 2026-04-05, branch: test/issue-218/block-sdk-e2e, session: 20260405-225642-t3-5-block-sdk-integration-test-218)
- [#217] Comprehensive block SDK developer guide covering all 5 block types, Tier 1/2 distribution, Collection transport, port system, configuration, and testing (@claude, 2026-04-05, branch: docs/issue-217/block-sdk-guide, session: 20260405-225639-t3-4-developer-documentation-for-block-s)
- [#215] BlockTestHarness utility for external block developers — validates block contracts, PackageInfo, entry-point callables, and smoke-test execution (@claude, 2026-04-05, branch: feat/issue-215/block-test-harness, session: 20260405-224022-t3-1-blocktestharness-utility-215)
- [#224] Write comprehensive project README.md covering architecture, features, tech stack, installation, and development setup (@claude, 2026-04-05, branch: docs/issue-224/readme-and-gitattributes, session: 20260405-223925-write-comprehensive-readme-md-and-fix-17)
- [#172] Add .gitattributes with CHANGELOG.md merge=union to prevent merge conflicts (@claude, 2026-04-05, branch: docs/issue-224/readme-and-gitattributes, session: 20260405-223925-write-comprehensive-readme-md-and-fix-17)

### Fixed

- [#233] Expose search_space in optimize-params API, wire endpoint to param_optimizer, remove dead NotImplementedError catch (@claude, 2026-04-05, branch: feat/issue-233/param-optimizer)
- [#206] Browse button supports file multi-select for Load blocks, folder select for Save blocks (@claude, 2026-04-06, branch: fix/issue-206-207-208/io-block-ux)
- [#207] Remove unused Format Override field from IO block config schema (@claude, 2026-04-06, branch: fix/issue-206-207-208/io-block-ux)
- [#208] Hide direction field in bottom Config panel for IO blocks to match canvas display (@claude, 2026-04-06, branch: fix/issue-206-207-208/io-block-ux)
- [#203] Handle asyncio.CancelledError in WebSocket handler for clean server shutdown (@claude, 2026-04-05, branch: fix/issue-203/ws-shutdown-hang, session: 20260405-212520-fix-websocket-shutdown-hang-by-handling)
- [#195] Guard panel resize persistence — minimum size threshold in onLayoutChanged + hydration validation in Zustand store (@claude, 2026-04-05, branch: fix/issue-195/panel-resize-persist, session: 20260405-205531-fix-panel-resize-broken-onlayoutchanged)
- [#194] Improve project dialog UX — remove useless "Optional note" field, add Browse button for native directory selection (@claude, 2026-04-05, branch: fix/issue-194/project-dialog-browse, session: 20260405-205530-fix-project-dialog-ux-remove-useless-fie)
- [#192] Fix post-redesign frontend: panel resize handles (12px hit area), block delete button, IO block split into Load/Save, drag ghost preview, port label row removal, horizontal scrollbar overflow (@claude, 2026-04-05, branch: fix/issue-192/post-redesign-frontend, session: 20260405-202012-fix-p0-post-redesign-frontend-issues-pan)
- [#179] Restrict CORS origins to localhost by default, configurable via SCIEASY_CORS_ORIGINS env var (@claude, 2026-04-05, branch: fix/issue-179/cors-origins, session: 20260405-193319-fix-179-restrict-cors-allow-origins-befo)
- [#178] Replace PPM image preview with PNG encoding for universal browser support (@claude, 2026-04-05, branch: fix/issue-178/png-image-preview, session: 20260405-192915-fix-178-replace-ppm-image-preview-with-p)
- [#180] Generate block config schema from Block class metadata instead of hardcoding (@claude, 2026-04-05, branch: fix/issue-180/block-config-schema, session: 20260405-193622-fix-180-generate-block-config-schema-fro)
- [#184] Align frontend GUI with ARCHITECTURE.md Section 9 spec: resizable 3-column layout, 3-part BlockNode with inline editable config, spec-correct port type colors, 6 bottom panel tabs, 14+ keyboard shortcuts, shadcn/ui integration, Projects dropdown toolbar (@claude, 2026-04-05, branch: fix/issue-184/frontend-spec-compliance, session: 20260405-192530-fix-critical-frontend-design-deviations)
- [#182] Add Vite dev server proxy for /api and /ws to fix HTML-instead-of-JSON error in dev mode (@claude, 2026-04-05, branch: fix/issue-182/vite-api-proxy, session: 20260405-182205-fix-frontend-dev-server-missing-api-prox)
- [#166] Create LineageRecorder component that persists LineageRecord on terminal block events via EventBus (@claude, 2026-04-05, branch: fix/batch-b2/issues-166-169, session: 20260405-163539-fix-issues-166-169-lineagerecorder-zarrb)
- [#167] Make ZarrBackend.write() atomic via write-to-temp-then-rename (@claude, 2026-04-05, branch: fix/batch-b2/issues-166-169, session: 20260405-163539-fix-issues-166-169-lineagerecorder-zarrb)
- [#168] Guard against Collection with None item_type in worker.py serialise_outputs (@claude, 2026-04-05, branch: fix/batch-b2/issues-166-169, session: 20260405-163539-fix-issues-166-169-lineagerecorder-zarrb)
- [#169] Replace silent except-continue in BlockRegistry with logged warnings (@claude, 2026-04-05, branch: fix/batch-b2/issues-166-169, session: 20260405-163539-fix-issues-166-169-lineagerecorder-zarrb)
- [#162] Replace blocking popen.communicate() with run_in_executor so asyncio event loop stays responsive during block execution (@claude, 2026-04-05, branch: fix/batch-b1/issues-162-163-164, session: 20260405-154742-fix-localrunner-async-blocking-dagschedu)
- [#163] Add PROCESS_EXITED subscription to DAGScheduler and align block_id between ProcessHandle and scheduler for crash detection (@claude, 2026-04-05, branch: fix/batch-b1/issues-162-163-164, session: 20260405-154742-fix-localrunner-async-blocking-dagschedu)
- [#164] Wire CheckpointManager into DAGScheduler pause/resume with auto-save on terminal events for crash recovery (@claude, 2026-04-05, branch: fix/batch-b1/issues-162-163-164, session: 20260405-154742-fix-localrunner-async-blocking-dagschedu)
- [#165] Fix Windows path assertions in test_adapters, test_data_object_persistence, and test_app_block after StorageReference POSIX normalization (@claude, 2026-04-05, branch: fix/issue-165/windows-test-paths, session: 20260405-155826-fix-windows-path-assertions-in-test-adap)
- [#120] Unwrap subprocess output envelope in LocalRunner so downstream sees port names at top level (@claude, 2026-04-05, branch: fix/batch-a3/issues-120-132-122, session: 20260405-014138-fix-subprocess-runner-output-pipeline-12)
- [#132] Preserve TypeSignature type_chain across subprocess boundary in worker serialization/reconstruction (@claude, 2026-04-05, branch: fix/batch-a3/issues-120-132-122, session: 20260405-014138-fix-subprocess-runner-output-pipeline-12)
- [#122] Fix unawaited async emit() in spawn_block_process by scheduling via create_task (@claude, 2026-04-05, branch: fix/batch-a3/issues-120-132-122, session: 20260405-014138-fix-subprocess-runner-output-pipeline-12)
- [#128] Remove self.transition() calls from Block.run() methods — state management is scheduler responsibility, not block (@claude, 2026-04-05, branch: fix/issue-128/remove-block-self-transition)
- [#119] Inject BlockRegistry into DAGScheduler — resolve NodeDef.block_type to Block instance before dispatch (@claude, 2026-04-05, branch: fix/batch-a2/issues-119-121, session: 20260405-003357-wave-a2-inject-blockregistry-into-dagsch)
- [#121] Pass BlockRegistry to CLI validate/run commands — enable type-compatibility and dangling-port checks (@claude, 2026-04-05, branch: fix/batch-a2/issues-119-121, session: 20260405-003357-wave-a2-inject-blockregistry-into-dagsch)
### Added

- [#216] Block package templates and `scieasy init-block-package` CLI scaffold command (ADR-026 Phase 3.2+3.3) (@claude, 2026-04-05, branch: feat/issue-216/block-package-templates, session: 20260405-223950-t3-2-3-3-block-package-templates-cli-sca)
- [#213] AdapterRegistry priority enforcement: external adapters cannot override built-in extensions (ADR-025 Phase 2.4) (@claude, 2026-04-05, branch: feat/issue-213/adapter-priority)
- [#212] TypeRegistry entry-points scanning for external type plugins (ADR-025 Phase 2.3) (@claude, 2026-04-05, branch: feat/issue-212/type-registry-entrypoints, session: 20260405-221125-typeregistry-entry-points-scanning-phase)
- [#211] PackageInfo dataclass and BlockRegistry callable entry-points protocol (ADR-025 Phase 2.1+2.2) (@claude, 2026-04-05, branch: feat/issue-211/package-info-registry)
- [#189] Frontend bundling, SPA serving, and `scieasy gui` command (ADR-024 Phase 1) (@claude, 2026-04-05, branch: feat/issue-189/frontend-bundling-gui, session: 20260405-194020-phase-1-frontend-bundling-and-scieasy-gu)
- [#64] AI generation templates and validation pipeline updated for Collection model (@claude, 2026-04-05, branch: feat/issue-64/ai-generation-templates, session: 20260405-014102-feat-ai-generation-templates-for-collect)
- [#127] Auto-flush data persistence pipeline: BackendRouter MRO resolution, flush_context, DataObject.save() with idempotency, _auto_flush() safety net (@claude, 2026-04-05, branch: fix/batch-a1/issues-127-49-52-67)
### Fixed

- [#125] Add AIBlock to _infer_category() so AIBlock subclasses get category "ai" instead of "unknown" (@claude, 2026-04-05, branch: fix/issue-125/aiblock-infer-category, session: 20260405-004900-issue-125-add-aiblock-to-infer-category)

### Removed

- [#59] Remove InputDelivery enum — MEMORY-only delivery via ADR-020 Collection auto-unpack (@claude, 2026-04-04, branch: fix/batch-1/issues-59-57-58-48, session: 20260404-192842-batch-1-remove-inputdelivery-postprocess)

### Changed

- [#55] Item-level hashing for Collection lineage — LineageRecord hash fields from list[str] to dict[str, list[str]], backward-compat store parsing, collection_hashes() utility (@claude, 2026-04-05, branch: feat/issue-55/collection-item-level-hashing, session: 20260405-004917-issue-55-item-level-hashing-for-collecti)
- [#123] Update Block.run() signature from dict[str, Any] to dict[str, Collection] across base + all subclasses, add architecture test; wrap MergeBlock/SplitBlock outputs in Collection (@claude, 2026-04-05, branch: fix/issue-123/block-run-collection-signature, session: 20260405-002441-issue-123-block-run-signature-dict-str-a)
- [#126] Sync ARCHITECTURE.md run() signature and ADR-017/018 status (@claude, 2026-04-05, branch: docs/issue-126/docs-sync-adr-architecture, session: 20260405-003356-docs-sync-architecture-md-and-adr-status)
- [#123] Update Block.run() signature from dict[str, Any] to dict[str, Collection] across base + all subclasses, add architecture test (@claude, 2026-04-05, branch: fix/issue-123/block-run-collection-signature, session: 20260405-002441-issue-123-block-run-signature-dict-str-a)
- [#57] Update Block.postprocess() type annotation to dict[str, Collection] (@claude, 2026-04-04, branch: fix/batch-1/issues-59-57-58-48, session: 20260404-192842-batch-1-remove-inputdelivery-postprocess)
- [#58] Document that port constraint functions receive Collection objects (@claude, 2026-04-04, branch: fix/batch-1/issues-59-57-58-48, session: 20260404-192842-batch-1-remove-inputdelivery-postprocess)
- [#124] Distinguish planned vs existing files in PROJECT_TREE.md (@claude, 2026-04-05, branch: docs/issue-124/project-tree-clarity, session: 20260405-003343-docs-project-tree-md-planned-vs-existing)

### Fixed

- [#51] Atomic write for FilesystemBackend; document Zarr/Composite write atomicity risks (@claude, 2026-04-05, branch: fix/issue-51/storage-write-atomicity, session: 20260405-004830-fix-storage-write-atomicity-on-cancel-cr)

### Fixed

- [#68] Move AppBlock exchange directory from system temp to project workspace (@claude, 2026-04-05, branch: fix/issue-68/appblock-exchange-dir, session: 20260405-004749-fix-appblock-exchange-dir-in-project-wor)

### Added

- [#159] Deliver Phase 7-8 project-first API and React workflow editor with live execution, previews, and frontend/backend test coverage (@Codex, 2026-04-05, branch: codex/phase7-8, session: 20260405-025125-phase-7-8-full-stack-delivery-api-layer)

- [#61] Selective re-run logic — reset_block() with dependency chain validation (@claude, 2026-04-05, branch: feat/issue-61/selective-rerun, session: 20260405-010832-feat-selective-re-run-reset-block-in-dag)
- [#60] SubWorkflowBlock nested subprocess cleanup — SIGTERM callback and Windows Job Object (@claude, 2026-04-05, branch: feat/issue-60/subworkflow-cleanup, session: 20260405-010910-feat-subworkflowblock-nested-subprocess)
- [#65] FastAPI lifespan shutdown terminates block subprocesses via ProcessRegistry (@claude, 2026-04-05, branch: feat/issue-65/fastapi-lifespan-shutdown, session: 20260405-010805-feat-fastapi-lifespan-shutdown-processre)
- [#54] Capture EnvironmentSnapshot inside subprocess for accurate lineage data (@claude, 2026-04-05, branch: feat/issue-54/env-snapshot-subprocess, session: 20260405-005852-feat-capture-environmentsnapshot-in-subp)
- [#62] Checkpoint intermediate_refs Collection serialization for resume support (@claude, 2026-04-05, branch: feat/issue-62/checkpoint-collection-refs, session: 20260405-005921-feat-checkpoint-intermediate-refs-collec)
- [#72] ResourceRequest max_internal_workers for accurate CPU accounting with parallel_map (@claude, 2026-04-05, branch: feat/issue-72/resource-request-workers, session: 20260405-005829-feat-resourcerequest-max-internal-worker)
- [#76] broadcast_apply memory guard, Array.__array__() protocol, in-memory scope docs (@claude, 2026-04-05, branch: feat/issue-76/broadcast-apply-improvements, session: 20260405-004843-feat-broadcast-apply-memory-guard-array)
- [#133] Add example workflow YAML files for CLI testing — raman_preprocessing, simple_merge, image_pipeline (@claude, 2026-04-05, branch: chore/issue-133/example-workflows, session: 20260405-002958-add-example-workflow-yaml-files-for-cli)
- [#48] Enforce JSON-serializable metadata on DataObject construction (@claude, 2026-04-04, branch: fix/batch-1/issues-59-57-58-48, session: 20260404-192842-batch-1-remove-inputdelivery-postprocess)

- [#113] Implement all ADR-017–022 TODO stubs, raise coverage threshold to 85% — resolve 38 TODOs across 32 files, implement WebSocket handler, ProcessExitedWithoutOutputError, create_reference(), Collection unpack/pack, CheckpointManager integration, ViewProxy.from_file(), add 24 new tests (@claude, 2026-04-04, branch: feat/issue-113/implement-all-todos-raise-coverage, session: 20260404-170726-implement-all-todo-stubs-adr-017-to-adr)
- [#109] Phase 6.2: Implement scieasy CLI commands — init, validate, run, blocks, serve with typer.testing smoke tests (@claude, 2026-04-04, branch: phase6/cli-commands)
- [#108] Phase 6.1: Workflow validator with type checking, cycle detection, dangling port detection — 6 ordered validation checks, 21 tests (@claude, 2026-04-04, branch: phase6/workflow-validator, session: 20260404-052508-phase-6-1-workflow-validator)
- [#107] Phase 6.1: YAML workflow serializer with Pydantic schema — load_yaml/save_yaml, schema validation, optional layout field, 10 tests (@claude, 2026-04-04, branch: phase6/workflow-serializer, session: 20260404-052427-phase-6-1-yaml-serializer-with-pydantic)
- [#105] Phase 5.8: Integration tests for execution engine — 23 tests exercising DAG+EventBus+Collection+Scheduler+cancellation+cycle detection end-to-end (@claude, 2026-04-04, branch: feat/issue-105/integration-tests, session: 20260404-051442-phase-5-8-integration-tests)
- [#102] Phase 5.7: SubWorkflowBlock scheduler factory injection and Collection passthrough — _scheduler_factory ClassVar for engine-layer injection (import-linter safe), fallback to _sequential_execute, Collections flow through unchanged, 3 new tests (@claude, 2026-04-04, branch: feat/issue-102/subworkflow-completion, session: 20260404-050108-phase-5-7-subworkflowblock-completion)
- [#101] Phase 5.4: Checkpoint serialization + CheckpointManager — ADR-018 JSON save/load with ISO-8601 timestamps, CheckpointManager directory-based storage with EventBus auto-subscription, skip_reasons for CANCELLED/SKIPPED states, 9 tests (@claude, 2026-04-04, branch: feat/issue-101/checkpoint-pause-resume, session: 20260404-050056-phase-5-4-checkpoint-serialization-check)
- [#98] Phase 5.1: DAG construction + event-driven DAGScheduler — ADR-018 DAG dataclass, build_dag, topological_sort (Kahn's), CycleError, skip propagation, pause/resume, input gathering, 38 tests (@claude, 2026-04-04, branch: feat/issue-8/dag-scheduler, session: 20260404-044439-phase-5-1-dag-construction-scheduling-ad)
- [#87] Phase 5.0c: IOBlock lazy Collection + adapter create_reference (ADR-020-Add2) -- FormatAdapter.create_reference() protocol, lazy StorageReference construction in IOBlock, Collection support in FileExchangeBridge.prepare(), ZarrAdapter scaffold (@claude, 2026-04-04, branch: feat/issue-87/ioblock-lazy-collection, session: 20260404-041549-phase-5-0c-ioblock-lazy-collection-adapt)
- [#96] Phase 5.2b: ProcessMonitor + LocalRunner + worker.py — ADR-017/019 async process death detection, subprocess-based block execution, worker entry point, RunHandle fields, 24 tests (@claude, 2026-04-04, branch: feat/issue-6/processmonitor-localrunner-worker, session: 20260404-043408-phase-5-2b-processmonitor-localrunner-wo)
- [#86] Phase 5.0b: LazyList + CodeBlock auto-unpack -- memory-safe lazy item loading, CodeBlock unpack/repack layer (@claude, 2026-04-04, branch: feat/issue-86/lazy-list-codeblock, session: 20260404-041450-phase-5-0b-lazylist-codeblock-auto-unpac)
- [#88] Phase 5.6: Collection operation blocks -- MergeCollection, SplitCollection, FilterCollection, SliceCollection (ADR-021) (@claude, 2026-04-04, branch: feat/issue-88/collection-operation-blocks, session: 20260404-041611-phase-5-6-collection-operation-blocks-ad)
- [#90] Phase 5.2a: PlatformOps + ProcessHandle + ProcessRegistry — ADR-017/019 cross-platform process lifecycle, PosixOps/WindowsOps, spawn_block_process factory, 29 tests (@claude, 2026-04-04, branch: feat/issue-90/platform-process-handle, session: 20260404-042159-phase-5-2a-platformops-processhandle-pro)
- [#91] Phase 5.3: ResourceManager with psutil watermark — ADR-022 OS-level memory monitoring, ADR-018 EventBus auto-release, GPU/CPU slot counting, 32 tests (@claude, 2026-04-04, branch: feat/issue-91/resource-management, session: 20260404-042212-phase-5-3-resource-management-adr-022)
- [#85] Phase 5.5: EventBus publish/subscribe dispatcher — ADR-018 runtime backbone with sync/async callback support, error isolation, 12 tests (@claude, 2026-04-04, branch: feat/issue-85/event-bus, session: 20260404-041317-phase-5-5-event-bus-implementation-adr-0)
- [#83] Phase 5.0a: Collection class + Block base utilities — ADR-020 Collection transport, pack/unpack/map_items/parallel_map, ProcessBlock Tier 1 process_item, port Collection transparency (@claude, 2026-04-04, branch: feat/issue-83/collection-block-utilities, session: 20260404-040318-phase-5-0a-collection-class-block-base-u)
- [#74] ADR-017-022 backbone scaffolding — TODO-annotated code stubs for subprocess isolation, event-driven scheduler, ProcessHandle, Collection transport, Collection operation blocks, psutil memory monitoring (@claude, 2026-04-04, branch: refactor/issue-74/adr-017-022-backbone, session: 20260404-024138-adr-017-022-backbone-scaffolding)
- [#42] Wave 3 tests: import coverage for Phase 5+ modules and extended block tests (@claude, 2026-04-03, branch: test/issue-42/wave3-import-coverage, session: 20260403-192429-wave-3-tests-import-coverage-and-extende)
- [#38] Wave 1 tests: 7 new test files for core and block coverage (~65% -> ~76%) (@claude, 2026-04-03, branch: test/issue-38/wave1-core-block-coverage, session: 20260403-191229-wave-1-tests-achieve-85-coverage-for-pha)
- [#35] Add AI PR review workflow with Codex agent (@claude, 2026-04-03, branch: feat/ai-pr-review, session: 20260403-162720-add-ai-pr-review-workflow-with-codex-age)
- [#32] Add code audit agent prompt for automated PR review (@claude, 2026-04-03, branch: feat/issue-32/code-audit-agent-prompt, session: 20260403-160517-add-code-audit-agent-prompt-for-automate)

- Phase 4 — Block system implementation:
  - Port system: type matching (isinstance-based, inheritance-aware), constraint validation, connection validation (source→target compatibility)
  - Block lifecycle: validate() with port type/constraint checking, postprocess() pass-through, state machine with transition()
  - IOBlock: read/write via AdapterRegistry with direction config
  - ProcessBlock: base with algorithm ClassVar; MergeBlock (Arrow concat), SplitBlock (head/ratio/filter)
  - CodeBlock: inline and script modes with MEMORY/PROXY/CHUNKED delivery
  - PythonRunner: exec() for inline, importlib for script with mtime-based hot-reload safety
  - introspect_script(): AST-based run() signature and configure() schema extraction
  - R/Julia runners: helpful NotImplementedError stubs
  - AppBlock: full lifecycle with RUNNING→PAUSED→RUNNING→DONE state transitions
  - FileExchangeBridge: prepare (JSON manifest), launch (subprocess), watch, collect
  - FileWatcher: polling-based output detection with timeout and glob patterns
  - SubWorkflowBlock: input/output mapping with sequential executor stub (real DAG scheduler in Phase 5)
  - BlockRegistry: Tier 1 (drop-in .py scan) + Tier 2 (entry_points) discovery, instantiate(), hot_reload()
  - AdapterRegistry: extension→adapter mapping with register_defaults() and entry_point scan
  - RunnerRegistry: language→runner mapping with register_defaults()
  - Format adapters: CSV (PyArrow), Parquet (PyArrow), TIFF (tifffile), generic binary (Artifact)
  - Stub adapters: mzXML, H5AD, FCS with NotImplementedError
  - 69 new tests across 7 test files covering ports, IO, process, code, app, registry, and subworkflow blocks
- CI: test coverage enforcement at 65% minimum via pytest-cov (will increase to 85% as test suite grows)
- CI: PR gate check requiring tests/ updates when src/ changes

- Phase 3 — Core data layer implementation:
  - DataObject metadata handling, TypeSignature auto-generation from class MRO
  - TypeSignature.matches() for inheritance-aware type compatibility
  - CompositeData slot management with type validation (get, set, slot_types)
  - TypeRegistry with register, resolve, scan_builtins, load_class, is_instance
  - ZarrBackend — write/read numpy arrays via Zarr, chunk-aware slicing
  - ArrowBackend — write/read PyArrow Tables via Parquet, column selection
  - FilesystemBackend — text and binary file storage with byte-range slicing
  - CompositeStore — directory-of-slots with manifest.json
  - ViewProxy — lazy loading with shape/axes metadata, size warnings (>2 GB)
  - LineageStore — SQLite-backed lineage records with ancestor tracing
  - EnvironmentSnapshot.capture() — Python version and key package versions
  - ProvenanceGraph — in-memory ancestry, descendant, diff, and audit queries
  - broadcast_apply() — named-axis-aware broadcasting utility
  - iter_axis_slices() — axis-aware slice generator for Array types
  - content_hash() — xxhash-based content hashing for lineage
  - 102 tests covering types, composites, storage round-trips, proxy lazy loading, lineage, and broadcast
- [#9] Scaffold repository structure and implement interface skeleton (Phases 0-2)
- [#7] Add mandatory branch and PR commit rules to CLAUDE.md
- Workflow gate system (`.workflow/gate.py`) for enforced development pipeline
- SpecKit integration (`.specify/`) for feature-level design pipeline
- GitHub Actions CI: lint, typecheck, test matrix, import contracts
- GitHub Actions workflow gate check for PR compliance
- Issue templates (feature, bug, refactor, architecture)
- PR template with checklist
- CLAUDE.md governance: branch discipline, merge conflict handling, branch cleanup rules
- CLAUDE.md Appendix C: bug fix and issue resolution workflow (ADR, spec, SpecKit, branch, PR/CI)
- Architecture documentation (`docs/architecture/`)
- ADR documentation (`docs/adr/`)

### Changed

- [#1] Bump actions/checkout from 4 to 6
- [#2] Bump astral-sh/setup-uv from 5 to 7
- [#3] Bump github/codeql-action from 3 to 4
- [#4] Bump actions/setup-python from 5 to 6

### Fixed

- [#414] Align `IOBlock.run()` with subclass-declared IO port names so plugin loaders like `LoadImage` no longer wrap payloads under the wrong key before reaching downstream blocks (@codex, 2026-04-08, branch: codex/fix-414-ioblock-ports, session: 20260408-180248-fix-414-align-ioblock-run-with-declared)
- [#406] Warn on stale editable-install `frontend/dist` bundles and fail CI when `frontend/src` is newer than the served SPA artifact (@codex, 2026-04-08, branch: codex/fix-403-406-gating, session: 20260408-154724-fix-406-scieasy-gui-serves-stale-fronten)
- [#403] Stop advertising abstract `IOBlock` as a `scieasy.blocks` entry-point and report abstract entry-points precisely in registry logs (@codex, 2026-04-08, branch: codex/fix-403-406-gating, session: 20260408-174206-fix-403-remove-abstract-ioblock-entry-po)
- [#70] AppBlock command injection guard + FileWatcher TOCTOU stability check (@claude, 2026-04-05, branch: fix/issue-70/appblock-security, session: 20260405-004432-fix-p1-appblock-command-injection-toctou)
- [#129] Fix port_accepts_type() Collection transparency bug in Block.validate() — pass Collection instance directly instead of type(value) (@claude, 2026-04-05, branch: fix/issue-129/port-accepts-type-collection, session: 20260405-004832-issue-129-fix-port-accepts-type-collecti)
- [#53] Normalize StorageReference.path to POSIX forward-slash format (@claude, 2026-04-05, branch: fix/issue-53/storage-ref-posix-paths, session: 20260405-004909-issue-53-storagereference-posix-path-nor)
- [#130] Filter original namespace keys and imported modules from PythonRunner.execute_inline() output (@claude, 2026-04-05, branch: fix/issue-130/python-runner-namespace-filter, session: 20260405-004852-issue-130-pythonrunner-execute-inline-fi)
- [#31] Deduplicate records in ProvenanceGraph descendants/ancestors traversals (@claude, 2026-04-04, branch: fix/issue-31/provenance-dedup, session: 20260404-034230-fix-provenancegraph-dedup-in-descendants)
- [#28] Make CompositeStore.slice() and iter_chunks() lazy -- load only requested slots (@claude, 2026-04-04, branch: fix/issue-28/composite-store-lazy, session: 20260404-034336-fix-compositestore-slice-and-iter-chunks)
- [#26] Persist axis metadata in ZarrBackend write/read for round-trip fidelity (@claude, 2026-04-04, branch: fix/issue-26/zarr-axes-metadata, session: 20260404-034314-fix-zarrbackend-to-persist-axis-metadata)
- [#27] Default LineageStore to persistent file path instead of in-memory (@claude, 2026-04-04, branch: fix/issue-27/lineage-default-path, session: 20260404-034315-fix-lineagestore-default-to-persistent-f)
- [#24] Enforce CompositeData slot contracts in TypeSignature matching and constructor (@claude, 2026-04-04, branch: fix/issue-24/composite-slot-contracts, session: 20260404-034408-fix-compositedata-slot-contracts-enforce)
- [#30] Include ndarray shape/dtype in content_hash to prevent lineage collisions (@claude, 2026-04-04, branch: fix/issue-30/content-hash-shape-dtype, session: 20260404-034204-fix-content-hash-to-include-ndarray-shap)
- [#16] Align BlockSpec and TypeRegistry with ADR-009 descriptor pattern
- [#14] Promote CHANGELOG CI check from warning to error
- CI: handle empty repo gracefully in workflows
- CI: disable `set -e` for pytest to capture exit code 5

### Removed
