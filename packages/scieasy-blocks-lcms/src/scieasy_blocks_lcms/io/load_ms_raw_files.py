"""Batch loader to Collection[MSRawFile] — records paths only; absorbs the deleted mzxml_adapter metadata extraction."""


class LoadMSRawFiles:
    """Placeholder for T-LCMS-003."""

    def __init__(self, *args, **kwargs):
        raise NotImplementedError(
            "T-LCMS-003: LoadMSRawFiles is a Phase 11 placeholder; see "
            "docs/specs/phase11-lcms-block-spec.md §9 T-LCMS-003."
        )
