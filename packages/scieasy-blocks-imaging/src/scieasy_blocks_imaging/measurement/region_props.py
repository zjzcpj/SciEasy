"""SINGLE block with multi-select properties param (checkbox UI) — replaces the OptEasy multi-block approach."""


class RegionProps:
    """Placeholder for T-IMG-024."""

    def __init__(self, *args: object, **kwargs: object) -> None:
        raise NotImplementedError(
            "T-IMG-024: RegionProps is a Phase 11 placeholder; see "
            "docs/specs/phase11-imaging-block-spec.md §9 T-IMG-024."
        )
