"""Unified image loader auto-detecting TIFF / OME-TIFF / PNG / JPG / Zarr / CZI / ND2 / LIF / npy."""


class LoadImage:
    """Placeholder for T-IMG-002."""

    def __init__(self, *args: object, **kwargs: object) -> None:
        raise NotImplementedError(
            "T-IMG-002: LoadImage is a Phase 11 placeholder; see docs/specs/phase11-imaging-block-spec.md §9 T-IMG-002."
        )
